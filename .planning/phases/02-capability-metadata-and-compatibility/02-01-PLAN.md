---
phase: 02-capability-metadata-and-compatibility
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/embeddings/capabilities.go
  - pkg/embeddings/embedding.go
autonomous: true
requirements:
  - CAPS-01
  - CAPS-02
user_setup: []
must_haves:
  truths:
    - "Providers can describe supported modalities, intents, and request-time options through shared capability metadata."
    - "Callers can inspect provider capabilities through one shared interface instead of provider-specific concrete types."
    - "Legacy EmbeddingFunction and MultimodalEmbeddingFunction method signatures remain unchanged."
  artifacts:
    - path: pkg/embeddings/capabilities.go
      provides: Shared capability metadata types and helper predicates
      min_lines: 80
      contains: "type CapabilityMetadata struct"
    - path: pkg/embeddings/embedding.go
      provides: Shared capability-reporting interface without breaking legacy interfaces
      contains: "type CapabilityAware interface"
  key_links:
    - from: pkg/embeddings/embedding.go
      to: pkg/embeddings/capabilities.go
      via: Shared capability interface returns the shared metadata type
      pattern: "Capabilities\\(\\) CapabilityMetadata"
---

<objective>
Define the shared capability metadata contract for Phase 2.

Purpose: Give providers and callers a provider-neutral way to describe and inspect supported modalities, intents, and request options without changing the legacy embedding interfaces.
Output: New shared capability metadata types plus an additive shared capability-reporting interface.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@.planning/phases/02-capability-metadata-and-compatibility/02-RESEARCH.md
@.planning/phases/02-capability-metadata-and-compatibility/02-VALIDATION.md
@CLAUDE.md
@pkg/embeddings/embedding.go
@pkg/embeddings/multimodal.go
@pkg/embeddings/registry.go
@pkg/embeddings/roboflow/roboflow.go
@pkg/api/v2/configuration.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared capability metadata types and helper predicates</name>
  <files>pkg/embeddings/capabilities.go</files>
  <read_first>pkg/embeddings/multimodal.go, pkg/embeddings/embedding.go, pkg/embeddings/registry.go, pkg/embeddings/roboflow/roboflow.go, pkg/api/v2/configuration.go, .planning/REQUIREMENTS.md, .planning/research/SUMMARY.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/CONCERNS.md, .planning/codebase/TESTING.md, .planning/phases/02-capability-metadata-and-compatibility/02-RESEARCH.md, .planning/phases/02-capability-metadata-and-compatibility/02-VALIDATION.md, CLAUDE.md</read_first>
  <action>Create `pkg/embeddings/capabilities.go` with a shared metadata model for provider capabilities. Define `type RequestOption string` with exported constants `RequestOptionDimension` and `RequestOptionProviderHints`. Define `type CapabilityMetadata struct { Modalities []Modality; Intents []Intent; RequestOptions []RequestOption; SupportsBatch bool; SupportsMixedPart bool }`. Add helper methods or functions that let callers check membership deterministically, such as `SupportsModality`, `SupportsIntent`, and `SupportsRequestOption`, while preserving slice-based metadata for stable serialization and inspection. Keep the metadata provider-neutral: do not add provider model names, config keys, or provider-specific task enums to this shared type.</action>
  <verify>rg -n "type RequestOption string|RequestOptionDimension|RequestOptionProviderHints|type CapabilityMetadata struct|SupportsMixedPart bool|SupportsBatch bool|func .*SupportsModality|func .*SupportsIntent|func .*SupportsRequestOption" pkg/embeddings/capabilities.go && go test ./pkg/embeddings</verify>
  <acceptance_criteria>
    - `pkg/embeddings/capabilities.go` contains `type RequestOption string`
    - `pkg/embeddings/capabilities.go` contains `RequestOptionDimension`
    - `pkg/embeddings/capabilities.go` contains `RequestOptionProviderHints`
    - `pkg/embeddings/capabilities.go` contains `type CapabilityMetadata struct`
    - `pkg/embeddings/capabilities.go` contains `SupportsBatch bool`
    - `pkg/embeddings/capabilities.go` contains `SupportsMixedPart bool`
    - `pkg/embeddings/capabilities.go` contains a `SupportsModality` helper
    - `go test ./pkg/embeddings` exits 0
  </acceptance_criteria>
  <done>`capabilities.go` exports the shared metadata shape needed for CAPS-01 and gives later adapters/providers a concrete contract to implement.</done>
</task>

<task type="auto">
  <name>Task 2: Add a shared capability-reporting interface without changing legacy embedding contracts</name>
  <files>pkg/embeddings/embedding.go</files>
  <read_first>pkg/embeddings/embedding.go, pkg/embeddings/capabilities.go, pkg/embeddings/multimodal.go, pkg/embeddings/registry.go, pkg/api/v2/configuration.go, .planning/REQUIREMENTS.md, .planning/phases/02-capability-metadata-and-compatibility/02-RESEARCH.md, .planning/phases/02-capability-metadata-and-compatibility/02-VALIDATION.md, CLAUDE.md</read_first>
  <action>Update `pkg/embeddings/embedding.go` to add an additive `CapabilityAware` interface with the exact method signature `Capabilities() CapabilityMetadata`. Place it alongside the existing embedding interfaces and comment that callers should type-assert this shared interface instead of provider-specific concrete types when inspecting modality, intent, or request-option support. Do not change the method sets or comments of `EmbeddingFunction`, `MultimodalEmbeddingFunction`, or `ContentEmbeddingFunction` beyond any minimal doc clarification needed to explain that capability discovery is additive.</action>
  <verify>rg -n "type CapabilityAware interface|Capabilities\\(\\) CapabilityMetadata|type EmbeddingFunction interface|type MultimodalEmbeddingFunction interface|type ContentEmbeddingFunction interface" pkg/embeddings/embedding.go && go test ./pkg/embeddings ./pkg/embeddings/roboflow</verify>
  <acceptance_criteria>
    - `pkg/embeddings/embedding.go` contains `type CapabilityAware interface`
    - `pkg/embeddings/embedding.go` contains `Capabilities() CapabilityMetadata`
    - `pkg/embeddings/embedding.go` still contains `type EmbeddingFunction interface`
    - `pkg/embeddings/embedding.go` still contains `type MultimodalEmbeddingFunction interface`
    - `pkg/embeddings/embedding.go` still contains `type ContentEmbeddingFunction interface`
    - `go test ./pkg/embeddings ./pkg/embeddings/roboflow` exits 0
  </acceptance_criteria>
  <done>`embedding.go` exposes shared capability discovery without breaking any existing public embedding interface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./pkg/embeddings`
- [ ] `go test ./pkg/embeddings ./pkg/embeddings/roboflow`
- [ ] `rg -n "type CapabilityMetadata struct|type CapabilityAware interface|RequestOptionDimension|RequestOptionProviderHints" pkg/embeddings/capabilities.go pkg/embeddings/embedding.go`
- [ ] No legacy embedding interface method signatures were removed or renamed
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Shared capability metadata exists in `pkg/embeddings`
- Callers can inspect capabilities through a shared interface only
- Legacy embedding interface signatures remain unchanged
  </success_criteria>

<output>
After completion, create `.planning/phases/02-capability-metadata-and-compatibility/02-01-SUMMARY.md`
</output>

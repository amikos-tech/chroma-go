---
phase: 01-shared-multimodal-contract
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-00"
  - "01-01"
files_modified:
  - pkg/embeddings/multimodal_validate.go
  - pkg/embeddings/multimodal_compat.go
autonomous: true
requirements:
  - MMOD-01
  - MMOD-03
  - MMOD-04
  - MMOD-05
user_setup: []
must_haves:
  truths:
    - "Invalid content, part, source, intent, and dimension shapes fail before provider I/O with typed validation errors."
    - "Validation checks structure only and never opens files or fetches URLs."
    - "Legacy ImageInput values can be adapted into the new shared part model without changing the old API."
  artifacts:
    - path: pkg/embeddings/multimodal_validate.go
      provides: Structural validation and typed validation error types
      min_lines: 100
      contains: "type ValidationError struct"
    - path: pkg/embeddings/multimodal_compat.go
      provides: Constructor and compatibility helpers for the shared contract
      min_lines: 60
      contains: "func NewImagePartFromImageInput"
  key_links:
    - from: pkg/embeddings/multimodal_validate.go
      to: pkg/embeddings/multimodal.go
      via: Validate methods enforce Content, Part, and BinarySource shape
      pattern: "func \\(c Content\\) Validate\\(\\) error"
    - from: pkg/embeddings/multimodal_compat.go
      to: pkg/embeddings/embedding.go
      via: Compatibility helper converts legacy ImageInput into Part
      pattern: "ImageInput"
---

<objective>
Implement structural validation and compatibility-safe constructors for the shared multimodal contract.

Purpose: Make the new request model usable and safe without changing Phase 1 into provider capability or transport work.
Output: Typed validation primitives plus helper constructors and legacy image bridge helpers.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/01-shared-multimodal-contract/01-CONTEXT.md
@.planning/phases/01-shared-multimodal-contract/01-RESEARCH.md
@.planning/phases/01-shared-multimodal-contract/01-VALIDATION.md
@docs/docs/embeddings.md
@pkg/embeddings/embedding.go
@pkg/embeddings/registry.go
@pkg/api/v2/configuration.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed structural validation for content, parts, sources, and request options</name>
  <files>pkg/embeddings/multimodal_validate.go</files>
  <read_first>pkg/embeddings/multimodal.go, pkg/embeddings/embedding.go, pkg/embeddings/registry.go, pkg/api/v2/configuration.go, .planning/REQUIREMENTS.md, .planning/research/SUMMARY.md, .planning/codebase/CONCERNS.md, .planning/codebase/ARCHITECTURE.md, docs/docs/embeddings.md, .planning/phases/01-shared-multimodal-contract/01-CONTEXT.md, .planning/phases/01-shared-multimodal-contract/01-RESEARCH.md, .planning/phases/01-shared-multimodal-contract/01-VALIDATION.md, pkg/embeddings/multimodal_validation_test.go</read_first>
  <action>Create `pkg/embeddings/multimodal_validate.go` with `ValidationIssue` and `ValidationError` types that implement `error` and surface multiple issues. Add `Validate()` methods for `Content`, `Part`, and `BinarySource`, plus a batch helper `ValidateContents(contents []Content) error`. Enforce structural rules only: `Content.Parts` must be non-empty; text parts require non-empty `Text` and `Source == nil`; image/audio/video/pdf parts require `Source != nil` and empty `Text`; `BinarySource.Kind` must be one of the defined source kinds and exactly one corresponding payload field must be populated; URL/file/base64 fields must be non-empty when selected; bytes payload must have `len(Bytes) > 0`; `Dimension` must be `> 0` and `<= math.MaxInt32`; `Intent` may be omitted, but non-empty values must survive trimming and whitespace-only values must fail. Do not call `os.Open`, `io.ReadAll`, `http.Get`, or any provider code from validation.</action>
  <verify>rg -n "type ValidationError struct|type ValidationIssue struct|func \\(c Content\\) Validate\\(\\) error|func \\(p Part\\) Validate\\(\\) error|func \\(s BinarySource\\) Validate\\(\\) error|func ValidateContents\\(contents \\[\\]Content\\) error|math.MaxInt32" pkg/embeddings/multimodal_validate.go && ! rg -n "os\\.Open|io\\.ReadAll|http\\.Get" pkg/embeddings/multimodal_validate.go && go test ./pkg/embeddings -run 'TestMultimodalIntentValidation|TestMultimodalValidationErrors|TestNewImagePartFromImageInput'</verify>
  <acceptance_criteria>
    - `pkg/embeddings/multimodal_validate.go` contains `type ValidationIssue struct`
    - `pkg/embeddings/multimodal_validate.go` contains `type ValidationError struct`
    - `pkg/embeddings/multimodal_validate.go` contains `func (c Content) Validate() error`
    - `pkg/embeddings/multimodal_validate.go` contains `func ValidateContents(contents []Content) error`
    - `pkg/embeddings/multimodal_validate.go` contains `math.MaxInt32`
    - `pkg/embeddings/multimodal_validate.go` does not contain `os.Open`
    - `pkg/embeddings/multimodal_validate.go` does not contain `http.Get`
  </acceptance_criteria>
  <done>`multimodal_validate.go` rejects invalid shared request shapes before provider I/O and does so with typed, multi-issue errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add constructors and compatibility helpers that preserve lazy source handling</name>
  <files>pkg/embeddings/multimodal_compat.go</files>
  <read_first>pkg/embeddings/multimodal.go, pkg/embeddings/multimodal_validate.go, pkg/embeddings/embedding.go, pkg/embeddings/registry.go, pkg/api/v2/configuration.go, .planning/REQUIREMENTS.md, .planning/research/SUMMARY.md, .planning/codebase/CONCERNS.md, .planning/codebase/ARCHITECTURE.md, docs/docs/embeddings.md, .planning/phases/01-shared-multimodal-contract/01-CONTEXT.md, .planning/phases/01-shared-multimodal-contract/01-VALIDATION.md, pkg/embeddings/multimodal_validation_test.go</read_first>
  <action>Create `pkg/embeddings/multimodal_compat.go` with helper constructors `NewTextPart(text string) Part`, `NewPartFromSource(modality Modality, source BinarySource) Part`, `NewBinarySourceFromURL(url string) BinarySource`, `NewBinarySourceFromFile(filePath string) BinarySource`, `NewBinarySourceFromBase64(base64Data string) BinarySource`, and `NewBinarySourceFromBytes(data []byte) BinarySource`. Add `NewImagePartFromImageInput(input ImageInput) (Part, error)` to bridge the legacy image-only type into the new shared contract by preserving URL/file/base64 provenance exactly as provided. Constructors may call shared validation but must never read files, decode remote URLs, or mutate `ImageInput` behavior.</action>
  <verify>rg -n "func NewTextPart\\(text string\\) Part|func NewPartFromSource\\(modality Modality, source BinarySource\\) Part|func NewBinarySourceFromFile\\(filePath string\\) BinarySource|func NewImagePartFromImageInput\\(input ImageInput\\) \\(Part, error\\)|input\\.Type\\(\\)" pkg/embeddings/multimodal_compat.go && ! rg -n "os\\.Open|http\\.Get" pkg/embeddings/multimodal_compat.go && go test ./pkg/embeddings -run 'TestMultimodalIntentValidation|TestMultimodalValidationErrors|TestNewImagePartFromImageInput'</verify>
  <acceptance_criteria>
    - `pkg/embeddings/multimodal_compat.go` contains `func NewTextPart(text string) Part`
    - `pkg/embeddings/multimodal_compat.go` contains `func NewPartFromSource(modality Modality, source BinarySource) Part`
    - `pkg/embeddings/multimodal_compat.go` contains `func NewBinarySourceFromFile(filePath string) BinarySource`
    - `pkg/embeddings/multimodal_compat.go` contains `func NewImagePartFromImageInput(input ImageInput) (Part, error)`
    - `pkg/embeddings/multimodal_compat.go` contains `input.Type()`
    - `pkg/embeddings/multimodal_compat.go` does not contain `os.Open`
    - `pkg/embeddings/multimodal_compat.go` does not contain `http.Get`
  </acceptance_criteria>
  <done>Constructors and compatibility helpers create valid shared parts while keeping file and URL sources lazy references.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./pkg/embeddings`
- [ ] `go test -tags=basicv2 ./pkg/api/v2 -run '^TestBuildEmbeddingFunctionFromConfig$'`
- [ ] `rg -n "func \\(c Content\\) Validate\\(\\) error|func NewImagePartFromImageInput\\(input ImageInput\\)|type ValidationError struct" pkg/embeddings/multimodal_validate.go pkg/embeddings/multimodal_compat.go`
- [ ] Validation code contains no file or network I/O calls
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Invalid shapes fail before provider I/O with typed validation errors
- Constructors preserve explicit source provenance and lazy URL/file handling
- Legacy `ImageInput` can be converted into a new shared `Part` without changing existing public APIs
  </success_criteria>

<output>
After completion, create `.planning/phases/01-shared-multimodal-contract/01-02-SUMMARY.md`
</output>

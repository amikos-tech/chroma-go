---
phase: 01-shared-multimodal-contract
plan: "01"
type: execute
wave: 1
depends_on:
  - "01-00"
files_modified:
  - pkg/embeddings/multimodal.go
  - pkg/embeddings/embedding.go
autonomous: true
requirements:
  - MMOD-01
  - MMOD-02
  - MMOD-03
  - MMOD-04
user_setup: []
must_haves:
  truths:
    - "Callers can build one multimodal semantic unit as a Content value with ordered Parts."
    - "The shared contract names explicit text, image, audio, video, and PDF modalities."
    - "Portable request options live on Content rather than hidden context keys."
  artifacts:
    - path: pkg/embeddings/multimodal.go
      provides: Shared multimodal request, part, source, modality, intent, and option types
      min_lines: 80
      contains: "type Content struct"
    - path: pkg/embeddings/embedding.go
      provides: Additive richer embedding interface without breaking legacy interfaces
      contains: "type ContentEmbeddingFunction interface"
  key_links:
    - from: pkg/embeddings/embedding.go
      to: pkg/embeddings/multimodal.go
      via: EmbedContent and EmbedContents signatures use Content
      pattern: "EmbedContents\\(ctx context\\.Context, contents \\[\\]Content\\)"
---

<objective>
Define the additive shared multimodal contract types for Phase 1.

Purpose: Establish a portable caller-facing request model in `pkg/embeddings` without breaking the existing text-only and image-only APIs.
Output: New shared multimodal type definitions plus an additive richer embedding interface.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/01-shared-multimodal-contract/01-CONTEXT.md
@.planning/phases/01-shared-multimodal-contract/01-RESEARCH.md
@docs/docs/embeddings.md
@pkg/embeddings/embedding.go
@pkg/embeddings/registry.go
@pkg/api/v2/configuration.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical multimodal request, source, and intent types</name>
  <files>pkg/embeddings/multimodal.go</files>
  <read_first>pkg/embeddings/embedding.go, pkg/embeddings/registry.go, .planning/REQUIREMENTS.md, .planning/research/SUMMARY.md, .planning/codebase/CONCERNS.md, .planning/codebase/ARCHITECTURE.md, docs/docs/embeddings.md, .planning/phases/01-shared-multimodal-contract/01-CONTEXT.md, .planning/phases/01-shared-multimodal-contract/01-RESEARCH.md, .planning/phases/01-shared-multimodal-contract/01-VALIDATION.md, pkg/embeddings/multimodal_test.go</read_first>
  <action>Create `pkg/embeddings/multimodal.go` with exported additive shared contract types. Define `Modality` constants `ModalityText`, `ModalityImage`, `ModalityAudio`, `ModalityVideo`, and `ModalityPDF`. Define string-backed `Intent` constants `IntentRetrievalQuery`, `IntentRetrievalDocument`, `IntentClassification`, `IntentClustering`, and `IntentSemanticSimilarity`. Define `SourceKind` constants `SourceKindURL`, `SourceKindFile`, `SourceKindBase64`, and `SourceKindBytes`. Add `type BinarySource struct { Kind SourceKind; URL string; FilePath string; Base64 string; Bytes []byte; MIMEType string }`, `type Part struct { Modality Modality; Text string; Source *BinarySource }`, and `type Content struct { Parts []Part; Intent Intent; Dimension *int; ProviderHints map[string]any }`. Keep parts ordered via slices only and do not add any map-based representation.</action>
  <verify>rg -n "type Content struct|Parts \\[\\]Part|type Part struct|ModalityPDF Modality = \"pdf\"|IntentSemanticSimilarity Intent = \"semantic_similarity\"|ProviderHints map\\[string\\]any" pkg/embeddings/multimodal.go && go test ./pkg/embeddings -run 'TestMultimodalContentSupportsAllModalities|TestMultimodalContentPreservesOrder|TestMultimodalRequestOptions'</verify>
  <acceptance_criteria>
    - `pkg/embeddings/multimodal.go` contains `type Content struct {`
    - `pkg/embeddings/multimodal.go` contains `Parts []Part`
    - `pkg/embeddings/multimodal.go` contains `type Part struct {`
    - `pkg/embeddings/multimodal.go` contains `ModalityPDF Modality = "pdf"`
    - `pkg/embeddings/multimodal.go` contains `IntentSemanticSimilarity Intent = "semantic_similarity"`
    - `pkg/embeddings/multimodal.go` contains `ProviderHints map[string]any`
  </acceptance_criteria>
  <done>`multimodal.go` exports the canonical request, part, source, modality, and intent types required for MMOD-01 through MMOD-04.</done>
</task>

<task type="auto">
  <name>Task 2: Add richer content embedding interface without changing legacy multimodal APIs</name>
  <files>pkg/embeddings/embedding.go</files>
  <read_first>pkg/embeddings/embedding.go, pkg/embeddings/multimodal.go, .planning/REQUIREMENTS.md, .planning/research/SUMMARY.md, .planning/codebase/CONCERNS.md, .planning/codebase/ARCHITECTURE.md, docs/docs/embeddings.md, .planning/phases/01-shared-multimodal-contract/01-CONTEXT.md, .planning/phases/01-shared-multimodal-contract/01-RESEARCH.md, .planning/phases/01-shared-multimodal-contract/01-VALIDATION.md, pkg/embeddings/multimodal_test.go</read_first>
  <action>Update `pkg/embeddings/embedding.go` to add a new additive `ContentEmbeddingFunction` interface that exposes `EmbedContents(ctx context.Context, contents []Content) ([]Embedding, error)` and `EmbedContent(ctx context.Context, content Content) (Embedding, error)`. Keep the existing `EmbeddingFunction` and `MultimodalEmbeddingFunction` definitions intact and unchanged. Add comments that this interface is the shared multimodal foundation for ordered mixed-part content, not a replacement for the legacy image-only interface in this phase.</action>
  <verify>rg -n "type ContentEmbeddingFunction interface|EmbedContents\\(ctx context.Context, contents \\[\\]Content\\) \\(\\[\\]Embedding, error\\)|EmbedContent\\(ctx context.Context, content Content\\) \\(Embedding, error\\)|type MultimodalEmbeddingFunction interface" pkg/embeddings/embedding.go && go test ./pkg/embeddings -run 'TestMultimodalContentSupportsAllModalities|TestMultimodalContentPreservesOrder|TestMultimodalRequestOptions'</verify>
  <acceptance_criteria>
    - `pkg/embeddings/embedding.go` contains `type ContentEmbeddingFunction interface {`
    - `pkg/embeddings/embedding.go` contains `EmbedContents(ctx context.Context, contents []Content) ([]Embedding, error)`
    - `pkg/embeddings/embedding.go` contains `EmbedContent(ctx context.Context, content Content) (Embedding, error)`
    - `pkg/embeddings/embedding.go` still contains `type MultimodalEmbeddingFunction interface {`
  </acceptance_criteria>
  <done>`embedding.go` exposes the additive content embedding interface while preserving the existing public interfaces unchanged.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./pkg/embeddings`
- [ ] `go test -tags=basicv2 ./pkg/api/v2 -run '^TestBuildEmbeddingFunctionFromConfig$'`
- [ ] `rg -n "type ContentEmbeddingFunction interface|type Content struct|IntentRetrievalDocument|SourceKindFile" pkg/embeddings/embedding.go pkg/embeddings/multimodal.go`
- [ ] No existing legacy interface signatures were removed from `pkg/embeddings/embedding.go`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Shared multimodal contract types exist in `pkg/embeddings`
- The additive interface exposes `EmbedContent` and `EmbedContents`
- Legacy `EmbeddingFunction` and `MultimodalEmbeddingFunction` remain public and unchanged in this phase
  </success_criteria>

<output>
After completion, create `.planning/phases/01-shared-multimodal-contract/01-01-SUMMARY.md`
</output>

---
phase: 08-document-gemini-and-voyageai-multimodal-embedding-functions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [D-01, D-02, D-10, D-11]

must_haves:
  truths:
    - "README mentions multimodal Content API as a capability"
    - "README mentions Gemini multimodal support"
    - "README mentions VoyageAI multimodal support"
    - "README examples table includes gemini_multimodal and voyage_multimodal entries"
    - "CHANGELOG.md exists with v0.4.1 release notes"
    - "ROADMAP.md phase 8 heading references VoyageAI instead of Nemotron"
    - "ROADMAP.md milestone goal references VoyageAI instead of vLLM/Nemotron"
    - "ROADMAP.md phase 7 description no longer contains 'Nemotron' parenthetical"
  artifacts:
    - path: "README.md"
      provides: "Updated features section with multimodal mentions"
      contains: "Content API"
    - path: "CHANGELOG.md"
      provides: "v0.4.1 release changelog"
      contains: "## [v0.4.1]"
    - path: ".planning/ROADMAP.md"
      provides: "Corrected phase name"
      contains: "VoyageAI"
  key_links:
    - from: "README.md"
      to: "docs/docs/embeddings/multimodal.md"
      via: "capability mention linking to Content API docs"
      pattern: "Content API|[Mm]ultimodal"
---

<objective>
Update README.md with multimodal capability mentions and new example entries, create CHANGELOG.md with v0.4.1 release notes, and correct the ROADMAP.md phase name from Nemotron to VoyageAI.

Purpose: Close the v0.4.1 milestone documentation: announce new capabilities in README, provide a changelog for the release, and fix naming consistency across planning artifacts.
Output: Updated README.md, new CHANGELOG.md, corrected ROADMAP.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-document-gemini-and-nemotron-multimodal-embedding-functions/08-CONTEXT.md
@.planning/phases/08-document-gemini-and-nemotron-multimodal-embedding-functions/08-RESEARCH.md

@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md with multimodal mentions and new example rows</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file -- lines 267-295 are "Embedding API and Models Support", lines 193-207 are the "Examples" table)
  </read_first>
  <action>
Edit README.md with the following changes (per D-10):

**1. Update Gemini line in "Embedding API and Models Support" section (around line 276):**

Change:
```
- [Google Gemini Embedding](https://go-client.chromadb.dev/embeddings/#google-gemini-ai) Support
```
To:
```
- [Google Gemini Embedding](https://go-client.chromadb.dev/embeddings/#google-gemini) Support (Multimodal: text, images, audio, video, PDF via Content API)
```

**2. Update VoyageAI line (around line 281):**

Change:
```
- [Voyage AI Embedding](https://go-client.chromadb.dev/embeddings/#voyage-ai) Support
```
To:
```
- [Voyage AI Embedding](https://go-client.chromadb.dev/embeddings/#voyage-ai) Support (Multimodal: text, images, video via Content API)
```

**3. Add Content API bullet in "Additional support features" section (after line 265, after the Array Metadata line):**

Add new line:
```
- ✅ [Multimodal Content API](https://go-client.chromadb.dev/embeddings/multimodal/) - Portable embedding interface for text, images, audio, video, and PDF with provider-neutral intents
```

**4. Add example rows in the "Examples" table (around line 207, after the last row):**

Add two rows:
```
| Gemini multimodal | [`examples/v2/gemini_multimodal`](./examples/v2/gemini_multimodal) | `main.go` | Gemini Content API with text + image |
| Voyage multimodal | [`examples/v2/voyage_multimodal`](./examples/v2/voyage_multimodal) | `main.go` | VoyageAI Content API with text + image |
```
  </action>
  <verify>
    <automated>grep -q "Multimodal Content API" README.md && grep -q "gemini_multimodal" README.md && grep -q "voyage_multimodal" README.md && grep -q "Multimodal: text, images, audio, video, PDF" README.md && grep -q "Multimodal: text, images, video" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains "Multimodal Content API" in the additional support features section
    - README.md Gemini line contains "Multimodal: text, images, audio, video, PDF via Content API"
    - README.md VoyageAI line contains "Multimodal: text, images, video via Content API"
    - README.md examples table contains a row with "gemini_multimodal" and path "./examples/v2/gemini_multimodal"
    - README.md examples table contains a row with "voyage_multimodal" and path "./examples/v2/voyage_multimodal"
    - Existing README content (compatibility section, installation, other features) is unchanged
  </acceptance_criteria>
  <done>README.md updated with multimodal capability mentions for Gemini and VoyageAI, Content API feature bullet, and two new example table rows</done>
</task>

<task type="auto">
  <name>Task 2: Create CHANGELOG.md and correct ROADMAP.md phase name</name>
  <files>CHANGELOG.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 13, 23, and 147 need Nemotron->VoyageAI correction)
  </read_first>
  <action>
**Part A: Create CHANGELOG.md (per D-11)**

Create a new file `CHANGELOG.md` in the repo root using Keep a Changelog format (keepachangelog.com). Content:

```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [v0.4.1] - 2026-03-23

### Added

- **Content API** - Portable multimodal embedding interface with `Content`, `Part`, and `BinarySource` types for embedding text, images, audio, video, and PDF content through a unified API (`EmbedContent`/`EmbedContents`).
- **Portable Intents** - Five provider-neutral intent constants (`IntentRetrievalQuery`, `IntentRetrievalDocument`, `IntentClassification`, `IntentClustering`, `IntentSemanticSimilarity`) that map to provider-specific task types.
- **Per-request Options** - `Dimension` and `ProviderHints` fields on `Content` for per-request configuration without mutating provider-wide settings.
- **Capability Metadata** - `CapabilityAware` interface for providers to declare supported modalities, intents, and request options. Callers can inspect capabilities without provider-specific type assertions.
- **Compatibility Adapters** - Automatic bridging between the Content API and legacy `EmbedDocuments`/`EmbedQuery` interfaces through the registry's `BuildContent` fallback chain.
- **Intent Mapping** - `IntentMapper` interface for providers to translate neutral intents to provider-native semantics with explicit errors for unsupported combinations.
- **Gemini Multimodal** - Gemini embedding function implements `ContentEmbeddingFunction`, `CapabilityAware`, and `IntentMapper` for text, image, audio, video, and PDF modalities. Default model updated to `gemini-embedding-2-preview`.
- **VoyageAI Multimodal** - VoyageAI embedding function implements `ContentEmbeddingFunction`, `CapabilityAware`, and `IntentMapper` for text, image, and video modalities via the `voyage-multimodal-3.5` model.
- **Registry Integration** - Content embedding functions can be built from stored configuration via `BuildContent`/`BuildContentCloseable` with full config round-trip support.
```

**Part B: Correct ROADMAP.md (per D-02, D-01)**

Edit `.planning/ROADMAP.md` with these changes:

1. Line 13 (milestone goal) -- change "vLLM/Nemotron" to "VoyageAI":
   From: `...then validate with Gemini and vLLM/Nemotron provider adoptions.`
   To: `...then validate with Gemini and VoyageAI provider adoptions.`

   NOTE: Line 13 already says "VoyageAI" -- verify before editing. If already correct, skip this change.

2. Line 23 (Phase 7 description) -- remove the Nemotron parenthetical:
   From: `- [ ] **Phase 7: Voyage Multimodal Adoption** - Wire VoyageAI into the shared multimodal contract with text, image, and video support to validate the foundation end-to-end. (pivoted from vLLM/Nemotron — vLLM lacks NVOmniEmbedModel support)`
   To: `- [x] **Phase 7: Voyage Multimodal Adoption** - Wire VoyageAI into the shared multimodal contract with text, image, and video support to validate the foundation end-to-end.`

   This removes the now-obsolete "(pivoted from vLLM/Nemotron ...)" parenthetical which is the last remaining Nemotron reference, and also marks Phase 7 as complete since it has been executed.

3. Line 147 (Phase 8 heading) -- verify it already says "VoyageAI". The ROADMAP was already partially corrected. If it still says "Nemotron", change it. Current content shows it already reads "Document Gemini and VoyageAI" on line 136, so this may already be correct.

4. Verify Phase 8 goal, requirements, and plan list are already populated (they were updated during planning). If any are still placeholders, update them:
   - Goal: `Update provider-specific documentation for Gemini and VoyageAI to show Content API multimodal usage, add runnable examples, update README and changelog to close the v0.4.1 milestone.`
   - Requirements: `[D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11]`
   - Plans: `2 plans`
  </action>
  <verify>
    <automated>test -f CHANGELOG.md && grep -q "v0.4.1" CHANGELOG.md && grep -q "Content API" CHANGELOG.md && grep -q "VoyageAI Multimodal" CHANGELOG.md && grep -q "Gemini Multimodal" CHANGELOG.md && ! grep -q "Nemotron" .planning/ROADMAP.md && grep -q "Document Gemini and VoyageAI" .planning/ROADMAP.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md exists in repo root
    - CHANGELOG.md contains "## [v0.4.1]" section header
    - CHANGELOG.md contains "Content API" under "### Added"
    - CHANGELOG.md contains "Gemini Multimodal" entry
    - CHANGELOG.md contains "VoyageAI Multimodal" entry
    - CHANGELOG.md contains "Portable Intents" entry
    - CHANGELOG.md contains "Capability Metadata" entry
    - CHANGELOG.md follows Keep a Changelog format (has header line referencing keepachangelog.com)
    - .planning/ROADMAP.md does NOT contain "Nemotron" anywhere (including Phase 7 line 23 parenthetical)
    - .planning/ROADMAP.md Phase 8 heading contains "VoyageAI"
    - .planning/ROADMAP.md Phase 8 goal is filled in (not "[To be planned]")
    - .planning/ROADMAP.md Phase 8 plans count shows "2 plans"
  </acceptance_criteria>
  <done>CHANGELOG.md created with comprehensive v0.4.1 release notes in Keep a Changelog format. ROADMAP.md corrected to reference VoyageAI everywhere -- no Nemotron references remain, including the Phase 7 line 23 parenthetical which was the last holdout.</done>
</task>

</tasks>

<verification>
- `test -f CHANGELOG.md` confirms changelog exists
- `grep "Nemotron" .planning/ROADMAP.md` returns no matches (including line 23)
- `grep "VoyageAI" .planning/ROADMAP.md` returns matches in milestone goal and Phase 8 heading
- `grep "gemini_multimodal\|voyage_multimodal" README.md` returns 2 matches
- `grep "Multimodal Content API" README.md` returns 1 match
- `make lint` passes
</verification>

<success_criteria>
- README.md announces multimodal capabilities for Gemini and VoyageAI, includes Content API feature bullet, and lists both new examples
- CHANGELOG.md documents v0.4.1 with all major additions (Content API, intents, capabilities, Gemini multimodal, VoyageAI multimodal)
- ROADMAP.md no longer references Nemotron anywhere (line 23 parenthetical removed), Phase 8 heading and goal are correct and complete
</success_criteria>

<output>
After completion, create `.planning/phases/08-document-gemini-and-nemotron-multimodal-embedding-functions/08-02-SUMMARY.md`
</output>

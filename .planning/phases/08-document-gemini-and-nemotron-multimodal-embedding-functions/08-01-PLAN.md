---
phase: 08-document-gemini-and-voyageai-multimodal-embedding-functions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/docs/embeddings.md
  - examples/v2/gemini_multimodal/main.go
  - examples/v2/voyage_multimodal/main.go
autonomous: true
requirements: [D-03, D-04, D-05, D-06, D-07, D-08, D-09]

must_haves:
  truths:
    - "Gemini section in embeddings.md lists gemini-embedding-2-preview as the default model"
    - "Gemini section has WithMaxFileSize in its option functions list"
    - "VoyageAI section in embeddings.md lists all 11 option functions"
    - "Both Gemini and VoyageAI sections have a Multimodal (Content API) subsection with EmbedContent examples"
    - "Runnable Gemini multimodal example exists at examples/v2/gemini_multimodal/main.go"
    - "Runnable VoyageAI multimodal example exists at examples/v2/voyage_multimodal/main.go"
    - "Existing text-only EmbedDocuments examples are unchanged in both provider sections"
  artifacts:
    - path: "docs/docs/embeddings.md"
      provides: "Updated Gemini and VoyageAI provider documentation with multimodal subsections"
      contains: "### Multimodal (Content API)"
    - path: "examples/v2/gemini_multimodal/main.go"
      provides: "Runnable Gemini Content API example"
      contains: "EmbedContent"
    - path: "examples/v2/voyage_multimodal/main.go"
      provides: "Runnable VoyageAI Content API example"
      contains: "EmbedContent"
  key_links:
    - from: "docs/docs/embeddings.md"
      to: "docs/docs/embeddings/multimodal.md"
      via: "cross-reference link in multimodal subsections"
      pattern: "multimodal\\.md"
---

<objective>
Update the Gemini and VoyageAI sections in embeddings.md with corrected default models, complete option function lists, and new "Multimodal (Content API)" subsections. Add standalone runnable examples for both providers.

Purpose: Close the provider documentation gap so users can discover and use multimodal Content API features through the main embeddings reference page.
Output: Updated embeddings.md with two new multimodal subsections, two new example programs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-document-gemini-and-nemotron-multimodal-embedding-functions/08-CONTEXT.md
@.planning/phases/08-document-gemini-and-nemotron-multimodal-embedding-functions/08-RESEARCH.md

@docs/docs/embeddings.md
@docs/docs/embeddings/multimodal.md
@pkg/embeddings/gemini/option.go
@pkg/embeddings/gemini/gemini.go
@pkg/embeddings/gemini/content.go
@pkg/embeddings/voyage/option.go
@pkg/embeddings/voyage/voyage.go
@pkg/embeddings/voyage/content.go
@examples/v2/embedding_function_basic/main.go

<interfaces>
<!-- Source-of-truth API surfaces from RESEARCH.md code examples -->

Gemini Option Functions (from pkg/embeddings/gemini/option.go):
- WithAPIKey(apiKey string)              -- Provide Gemini API key directly.
- WithEnvAPIKey()                        -- Load API key from GEMINI_API_KEY.
- WithAPIKeyFromEnvVar(envVar string)    -- Load API key from a custom env var.
- WithDefaultModel(model)                -- Set model. Default: gemini-embedding-2-preview.
- WithTaskType(taskType TaskType)        -- Set embedding task type.
- WithDimension(dimension int)           -- Set reduced output dimensionality.
- WithMaxBatchSize(maxBatchSize int)     -- Upper bound on items per embedding call.
- WithClient(client *genai.Client)       -- Provide a preconfigured genai client.
- WithMaxFileSize(maxBytes int64)        -- Max payload size for inline binary sources.

VoyageAI Option Functions (from pkg/embeddings/voyage/option.go):
- WithDefaultModel(model)                -- Set model. Default (text): voyage-2.
- WithMaxBatchSize(size int)             -- Upper bound (max 128).
- WithDefaultHeaders(headers)            -- Custom HTTP headers.
- WithAPIKey(apiToken string)            -- Provide API key directly.
- WithEnvAPIKey()                        -- Load API key from VOYAGE_API_KEY.
- WithAPIKeyFromEnvVar(envVar string)    -- Load API key from a custom env var.
- WithHTTPClient(client *http.Client)    -- Custom HTTP client.
- WithTruncation(truncation bool)        -- Enable/disable input truncation.
- WithEncodingFormat(format)             -- Response encoding format (e.g., base64).
- WithBaseURL(baseURL string)            -- Custom API base URL.
- WithInsecure()                         -- Allow HTTP (no TLS). Dev/testing only.

Gemini Content API usage:
```go
ef, err := gemini.NewGeminiEmbeddingFunction(gemini.WithEnvAPIKey())
content := embeddings.Content{
    Parts: []embeddings.Part{
        embeddings.NewTextPart("A cat sitting on a windowsill"),
        embeddings.NewPartFromSource(
            embeddings.ModalityImage,
            embeddings.NewBinarySourceFromURL("https://example.com/cat.jpg"),
        ),
    },
}
emb, err := ef.EmbedContent(ctx, content)
```

VoyageAI Content API usage:
```go
ef, err := voyage.NewVoyageAIEmbeddingFunction(
    voyage.WithEnvAPIKey(),
    voyage.WithDefaultModel("voyage-multimodal-3.5"),
)
content := embeddings.Content{
    Parts: []embeddings.Part{
        embeddings.NewTextPart("A dog running on a beach"),
        embeddings.NewPartFromSource(
            embeddings.ModalityImage,
            embeddings.NewBinarySourceFromURL("https://example.com/dog.jpg"),
        ),
    },
}
emb, err := ef.EmbedContent(ctx, content)
```

Capability table:
| Provider | Model | Modalities | Mixed-Part |
|----------|-------|------------|------------|
| Gemini | gemini-embedding-2-preview | text, image, audio, video, PDF | yes |
| Gemini | gemini-embedding-001 (legacy) | text only | no |
| VoyageAI | voyage-multimodal-3.5 | text, image, video | yes |
| VoyageAI | voyage-2 (default text) | text only | no |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Gemini and VoyageAI sections in embeddings.md</name>
  <files>docs/docs/embeddings.md</files>
  <read_first>
    - docs/docs/embeddings.md (full file -- lines 361-445 are the VoyageAI and Gemini sections)
    - docs/docs/embeddings/multimodal.md (cross-reference target -- note the relative link path)
    - pkg/embeddings/gemini/option.go (source of truth for Gemini options)
    - pkg/embeddings/voyage/option.go (source of truth for VoyageAI options)
  </read_first>
  <action>
Edit docs/docs/embeddings.md with the following changes. Do NOT restructure existing sections or remove existing text-only examples (per D-04).

**VoyageAI section (starts at line 361 "## Voyage AI"):**

1. After the intro paragraph and before the code example, add a "Supported Embedding Function Options" list (per D-07):

```
Supported Embedding Function Options:

- `WithAPIKey` - Provide Voyage API key directly.
- `WithEnvAPIKey` - Load API key from `VOYAGE_API_KEY`.
- `WithAPIKeyFromEnvVar` - Load API key from a custom environment variable.
- `WithDefaultModel` - Set the Voyage model to use. Default is `voyage-2`.
- `WithMaxBatchSize` - Set an upper bound on documents per embedding call (max 128).
- `WithDefaultHeaders` - Set custom HTTP headers.
- `WithHTTPClient` - Provide a custom HTTP client.
- `WithTruncation` - Enable or disable input truncation.
- `WithEncodingFormat` - Set the response encoding format.
- `WithBaseURL` - Set a custom API base URL.
- `WithInsecure` - Allow HTTP without TLS (development and testing only).
```

2. After the existing `EmbedDocuments` code example (the closing triple-backtick around line 394), add a new subsection:

```markdown
### Multimodal (Content API)

VoyageAI supports multimodal embeddings through the [Content API](embeddings/multimodal.md). The `voyage-multimodal-3.5` model accepts text, image, and video inputs.

` ` `go
package main

import (
	"context"
	"fmt"
	"log"

	"github.com/amikos-tech/chroma-go/pkg/embeddings"
	voyage "github.com/amikos-tech/chroma-go/pkg/embeddings/voyage"
)

func main() {
	ef, err := voyage.NewVoyageAIEmbeddingFunction(
		voyage.WithEnvAPIKey(),
		voyage.WithDefaultModel("voyage-multimodal-3.5"),
	)
	if err != nil {
		log.Fatalf("Error creating Voyage embedding function: %s", err)
	}

	content := embeddings.Content{
		Parts: []embeddings.Part{
			embeddings.NewTextPart("A dog running on a beach"),
			embeddings.NewPartFromSource(
				embeddings.ModalityImage,
				embeddings.NewBinarySourceFromURL("https://example.com/dog.jpg"),
			),
		},
	}
	emb, err := ef.EmbedContent(context.Background(), content)
	if err != nil {
		log.Fatalf("Error embedding content: %s", err)
	}
	fmt.Printf("Embedding dimension: %d\n", len(emb.ArrayOfFloat32))
}
` ` `
```

(Use real triple backticks, not the escaped ones shown above.)

**Gemini section (starts at line 396 "## Google Gemini"):**

3. Update the intro text (line 401) from:
   `gemini-embedding-001` is the default model.
   to:
   `gemini-embedding-2-preview` is the default model. The legacy `gemini-embedding-001` model is still available via `WithDefaultModel`.
   (per D-06)

4. Update the option functions list (lines 404-413):
   - Change `WithDefaultModel` description from "Default is `gemini-embedding-001`" to "Default is `gemini-embedding-2-preview`"
   - Add `WithMaxFileSize` at the end of the list: `- \`WithMaxFileSize\` - Set the maximum payload size for inline binary sources.`

5. In the existing Gemini code example (lines 415-445), update the model in the `WithDefaultModel` call from `"gemini-embedding-001"` to `"gemini-embedding-2-preview"` (per D-06).

6. After the existing Gemini code example (the closing triple-backtick around line 445), add a new subsection:

```markdown
### Multimodal (Content API)

Gemini supports multimodal embeddings through the [Content API](embeddings/multimodal.md). The `gemini-embedding-2-preview` model accepts text, image, audio, video, and PDF inputs.

` ` `go
package main

import (
	"context"
	"fmt"
	"log"

	"github.com/amikos-tech/chroma-go/pkg/embeddings"
	gemini "github.com/amikos-tech/chroma-go/pkg/embeddings/gemini"
)

func main() {
	ef, err := gemini.NewGeminiEmbeddingFunction(gemini.WithEnvAPIKey())
	if err != nil {
		log.Fatalf("Error creating Gemini embedding function: %s", err)
	}

	content := embeddings.Content{
		Parts: []embeddings.Part{
			embeddings.NewTextPart("A cat sitting on a windowsill"),
			embeddings.NewPartFromSource(
				embeddings.ModalityImage,
				embeddings.NewBinarySourceFromURL("https://example.com/cat.jpg"),
			),
		},
	}
	emb, err := ef.EmbedContent(context.Background(), content)
	if err != nil {
		log.Fatalf("Error embedding content: %s", err)
	}
	fmt.Printf("Embedding dimension: %d\n", len(emb.ArrayOfFloat32))
}
` ` `
```

(Use real triple backticks, not the escaped ones shown above.)
  </action>
  <verify>
    <automated>grep -c "### Multimodal (Content API)" docs/docs/embeddings.md | grep -q "2" && grep -q "gemini-embedding-2-preview" docs/docs/embeddings.md && grep -q "WithMaxFileSize" docs/docs/embeddings.md && grep -q "WithTruncation" docs/docs/embeddings.md && grep -q "voyage-multimodal-3.5" docs/docs/embeddings.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - docs/docs/embeddings.md contains exactly 2 occurrences of "### Multimodal (Content API)"
    - docs/docs/embeddings.md contains "gemini-embedding-2-preview" in the Gemini intro, option list, and code example
    - docs/docs/embeddings.md does NOT contain "Default is `gemini-embedding-001`" in the option list (replaced with gemini-embedding-2-preview)
    - docs/docs/embeddings.md contains "WithMaxFileSize" in the Gemini option list
    - docs/docs/embeddings.md VoyageAI section contains "WithTruncation", "WithEncodingFormat", "WithBaseURL", "WithInsecure", "WithHTTPClient" in option list
    - docs/docs/embeddings.md contains "voyage-multimodal-3.5" in VoyageAI multimodal subsection
    - docs/docs/embeddings.md contains cross-reference link "[Content API](embeddings/multimodal.md)" in both multimodal subsections
    - The existing VoyageAI EmbedDocuments code example (with "voyage-large-2") is still present
    - The existing Gemini EmbedDocuments code example structure is still present (with model updated to gemini-embedding-2-preview)
  </acceptance_criteria>
  <done>Both provider sections updated with option functions, corrected default models, and multimodal Content API subsections containing EmbedContent code examples and cross-links to multimodal.md</done>
</task>

<task type="auto">
  <name>Task 2: Add runnable multimodal example programs</name>
  <files>examples/v2/gemini_multimodal/main.go, examples/v2/voyage_multimodal/main.go</files>
  <read_first>
    - examples/v2/embedding_function_basic/main.go (established example pattern: package main, imports, log.Fatalf for errors)
    - pkg/embeddings/gemini/gemini.go (constructor signature, EmbedContent method)
    - pkg/embeddings/voyage/voyage.go (constructor signature, EmbedContent method)
  </read_first>
  <action>
Create two new example directories and main.go files following the established pattern from embedding_function_basic/main.go but focused on the Content API.

**examples/v2/gemini_multimodal/main.go** (per D-08):

```go
package main

import (
	"context"
	"fmt"
	"log"

	"github.com/amikos-tech/chroma-go/pkg/embeddings"
	gemini "github.com/amikos-tech/chroma-go/pkg/embeddings/gemini"
)

func main() {
	// Create a Gemini embedding function using the default multimodal model (gemini-embedding-2-preview).
	// Set GEMINI_API_KEY in your environment before running.
	ef, err := gemini.NewGeminiEmbeddingFunction(gemini.WithEnvAPIKey())
	if err != nil {
		log.Fatalf("Error creating embedding function: %s", err)
	}

	// Embed a single content item with text and an image.
	content := embeddings.Content{
		Parts: []embeddings.Part{
			embeddings.NewTextPart("A cat sitting on a windowsill"),
			embeddings.NewPartFromSource(
				embeddings.ModalityImage,
				embeddings.NewBinarySourceFromURL("https://upload.wikimedia.org/wikipedia/commons/thumb/3/3a/Cat03.jpg/1200px-Cat03.jpg"),
			),
		},
	}
	emb, err := ef.EmbedContent(context.Background(), content)
	if err != nil {
		log.Fatalf("Error embedding content: %s", err)
	}
	fmt.Printf("Single content embedding dimension: %d\n", len(emb.ArrayOfFloat32))

	// Embed a batch of content items with different modalities.
	contents := []embeddings.Content{
		{Parts: []embeddings.Part{embeddings.NewTextPart("A dog playing fetch")}},
		{Parts: []embeddings.Part{
			embeddings.NewPartFromSource(
				embeddings.ModalityImage,
				embeddings.NewBinarySourceFromURL("https://upload.wikimedia.org/wikipedia/commons/thumb/2/26/YellowLabradorLooking_new.jpg/1200px-YellowLabradorLooking_new.jpg"),
			),
		}},
	}
	results, err := ef.EmbedContents(context.Background(), contents)
	if err != nil {
		log.Fatalf("Error embedding contents: %s", err)
	}
	fmt.Printf("Batch results: %d embeddings\n", len(results))
}
```

**examples/v2/voyage_multimodal/main.go** (per D-09):

```go
package main

import (
	"context"
	"fmt"
	"log"

	"github.com/amikos-tech/chroma-go/pkg/embeddings"
	voyage "github.com/amikos-tech/chroma-go/pkg/embeddings/voyage"
)

func main() {
	// Create a VoyageAI embedding function with the multimodal model.
	// Set VOYAGE_API_KEY in your environment before running.
	ef, err := voyage.NewVoyageAIEmbeddingFunction(
		voyage.WithEnvAPIKey(),
		voyage.WithDefaultModel("voyage-multimodal-3.5"),
	)
	if err != nil {
		log.Fatalf("Error creating embedding function: %s", err)
	}

	// Embed a single content item with text and an image.
	content := embeddings.Content{
		Parts: []embeddings.Part{
			embeddings.NewTextPart("A dog running on a beach"),
			embeddings.NewPartFromSource(
				embeddings.ModalityImage,
				embeddings.NewBinarySourceFromURL("https://upload.wikimedia.org/wikipedia/commons/thumb/2/26/YellowLabradorLooking_new.jpg/1200px-YellowLabradorLooking_new.jpg"),
			),
		},
	}
	emb, err := ef.EmbedContent(context.Background(), content)
	if err != nil {
		log.Fatalf("Error embedding content: %s", err)
	}
	fmt.Printf("Single content embedding dimension: %d\n", len(emb.ArrayOfFloat32))

	// Embed a batch of content items with different modalities.
	contents := []embeddings.Content{
		{Parts: []embeddings.Part{embeddings.NewTextPart("Ocean waves crashing on rocks")}},
		{Parts: []embeddings.Part{
			embeddings.NewPartFromSource(
				embeddings.ModalityImage,
				embeddings.NewBinarySourceFromURL("https://upload.wikimedia.org/wikipedia/commons/thumb/3/3a/Cat03.jpg/1200px-Cat03.jpg"),
			),
		}},
	}
	results, err := ef.EmbedContents(context.Background(), contents)
	if err != nil {
		log.Fatalf("Error embedding contents: %s", err)
	}
	fmt.Printf("Batch results: %d embeddings\n", len(results))
}
```
  </action>
  <verify>
    <automated>test -f examples/v2/gemini_multimodal/main.go && test -f examples/v2/voyage_multimodal/main.go && grep -q "EmbedContent" examples/v2/gemini_multimodal/main.go && grep -q "EmbedContent" examples/v2/voyage_multimodal/main.go && grep -q "voyage-multimodal-3.5" examples/v2/voyage_multimodal/main.go && grep -q "package main" examples/v2/gemini_multimodal/main.go && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - examples/v2/gemini_multimodal/main.go exists and contains "package main"
    - examples/v2/gemini_multimodal/main.go contains "EmbedContent" and "EmbedContents"
    - examples/v2/gemini_multimodal/main.go contains "gemini.WithEnvAPIKey()"
    - examples/v2/gemini_multimodal/main.go contains "embeddings.ModalityImage"
    - examples/v2/gemini_multimodal/main.go uses log.Fatalf for error handling (not panic)
    - examples/v2/voyage_multimodal/main.go exists and contains "package main"
    - examples/v2/voyage_multimodal/main.go contains "EmbedContent" and "EmbedContents"
    - examples/v2/voyage_multimodal/main.go contains "voyage-multimodal-3.5"
    - examples/v2/voyage_multimodal/main.go contains "voyage.WithEnvAPIKey()"
    - examples/v2/voyage_multimodal/main.go uses log.Fatalf for error handling (not panic)
  </acceptance_criteria>
  <done>Two runnable multimodal example programs created following established example patterns, demonstrating single-content and batch Content API usage with text and image inputs</done>
</task>

</tasks>

<verification>
- `grep -c "### Multimodal (Content API)" docs/docs/embeddings.md` returns 2
- `grep "gemini-embedding-2-preview" docs/docs/embeddings.md` shows updated default model references
- `grep "WithMaxFileSize" docs/docs/embeddings.md` shows new Gemini option
- `grep -c "WithTruncation\|WithEncodingFormat\|WithBaseURL\|WithInsecure" docs/docs/embeddings.md` returns 4 (VoyageAI options)
- Both example files exist and compile conceptually (imports match real packages)
- `make lint` passes
</verification>

<success_criteria>
- embeddings.md has updated Gemini default model, complete option lists for both providers, and two new Multimodal (Content API) subsections
- Two new runnable example programs exist in examples/v2/
- Existing text-only documentation and code examples are preserved unchanged
- Cross-references to multimodal.md are present in both multimodal subsections
</success_criteria>

<output>
After completion, create `.planning/phases/08-document-gemini-and-nemotron-multimodal-embedding-functions/08-01-SUMMARY.md`
</output>

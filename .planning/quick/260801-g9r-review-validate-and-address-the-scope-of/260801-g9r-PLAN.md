---
phase: quick-260801-g9r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/api/v2/rank.go
  - pkg/api/v2/search.go
  - pkg/api/v2/rank_test.go
  - pkg/api/v2/search_test.go
  - pkg/api/v2/collection_http_test.go
autonomous: true
requirements:
  - GH-516

must_haves:
  truths:
    - "WithRank rejects every malformed built-in Rank, including invalid nested built-ins, before changing SearchRequest.Rank"
    - "Valid caller-defined Rank implementations remain accepted without adding a method to the public Rank interface"
    - "WithSearchFilter eagerly rejects every invalid non-nil Where tree before changing SearchRequest.Filter"
    - "Empty, IDs-only, nil-Where, and typed-nil-Where SearchFilter values retain their accepted behavior"
    - "Collection.Search reports invalid rank and filter options as option-application errors and performs no HTTP request"
  artifacts:
    - path: "pkg/api/v2/rank.go"
      provides: "KNN structural validation and one exact-built-in recursive validation boundary"
      contains: "func validateBuiltInRank"
    - path: "pkg/api/v2/search.go"
      provides: "Validate-before-assignment behavior for WithRank and WithSearchFilter"
      contains: "func (o *rankOption) ApplyToSearchRequest"
    - path: "pkg/api/v2/rank_test.go"
      provides: "Direct KNN validation and valid wire-format regressions"
      contains: "TestKnnRankValidation"
    - path: "pkg/api/v2/search_test.go"
      provides: "Option timing, recursive validation, compatibility, and no-mutation regressions"
      contains: "TestWithRank"
    - path: "pkg/api/v2/collection_http_test.go"
      provides: "Request-path proof that invalid options fail before transport"
      contains: "TestCollectionSearchRejectsInvalidOptionsBeforeSend"
  key_links:
    - from: "pkg/api/v2/search.go:rankOption.ApplyToSearchRequest"
      to: "pkg/api/v2/rank.go:validateBuiltInRank"
      via: "validate the exact built-in rank before assigning req.Rank"
      pattern: "validateBuiltInRank\\(o\\.rank\\)"
    - from: "pkg/api/v2/rank.go:validateBuiltInRank"
      to: "pkg/api/v2/rank.go:marshalRank"
      via: "reuse the existing recursive marshal guards rather than maintain a second tree walker"
      pattern: "marshalRank\\(rank, 0\\)"
    - from: "pkg/api/v2/rank.go:KnnRank.MarshalJSON"
      to: "pkg/api/v2/rank.go:KnnRank.Validate"
      via: "enforce query, key, and limit invariants for direct and nested KNN serialization"
      pattern: "k\\.Validate\\(\\)"
    - from: "pkg/api/v2/search.go:searchFilterOption.ApplyToSearchRequest"
      to: "pkg/api/v2/where.go:WhereClause.Validate"
      via: "validate every non-nil Where tree before assigning req.Filter"
      pattern: "o\\.filter\\.Where\\.Validate\\(\\)"
    - from: "pkg/api/v2/collection_http_test.go:TestCollectionSearchRejectsInvalidOptionsBeforeSend"
      to: "pkg/api/v2/collection_http.go:CollectionImpl.Search"
      via: "exercise NewSearchRequest options through the public request path and assert the server counter remains zero"
      pattern: "error applying search option"
---

<objective>
Close issue 516 by moving built-in rank and search-filter validation to the option boundary.

Purpose: malformed search options must fail when applied, before they mutate the request or become misleading serialization/transport errors, while the externally implementable Rank contract and valid filter edge cases remain compatible.

Output: recursive built-in rank validation, KNN structural validation, eager Where validation, focused option regressions, and an HTTP-boundary no-send regression.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/quick/260801-g9r-review-validate-and-address-the-scope-of/260801-g9r-CONTEXT.md
@.planning/quick/260801-g9r-review-validate-and-address-the-scope-of/260801-g9r-RESEARCH.md
@pkg/api/v2/rank.go
@pkg/api/v2/search.go
@pkg/api/v2/rank_test.go
@pkg/api/v2/search_test.go
@pkg/api/v2/collection_http.go
@pkg/api/v2/collection_http_test.go
@pkg/api/v2/options.go
@pkg/api/v2/where.go

**Working directory:** `/Users/tazarov/GolandProjects/chroma-go`

**Decision IDs used for traceability, in source order:**
- D-01: Rank validation contract.
- D-02: Filter behavior.
- D-03: Error attribution scope.
- D-04: Agent's discretion over the smallest validation design and focused tests.

**Scope boundaries:**
- Keep the public `Rank` interface unchanged per D-01. Do not require caller-defined ranks to expose `Validate`, and do not eagerly invoke a standalone custom rank's marshaler.
- Validate exact built-in rank types recursively by reusing `marshalRank`; do not add a parallel tree walker, reflection helper, or dependency per D-04.
- Preserve top-level `ErrNilRank` and `ErrNilFilter` precedence, and assign neither request field after a validation failure per D-01 and D-02.
- Preserve empty, IDs-only, untyped-nil-Where, and typed-nil-Where filters per D-02. Do not rewrite the caller-owned filter.
- Leave `CollectionImpl.Search`, `APIClientV2.ExecuteRequest`, and all HTTP error wrapping unchanged per D-03. Research found no behavior-preserving stage discriminator, and eager option validation resolves the reported paths before HTTP work starts.
- Keep direct `SearchRequest`/`SearchQuery` construction behavior outside this option-boundary change; issue 516 is closed through `WithRank` and `WithSearchFilter`.
- Add no package, schema, persistence, configuration, or external-service change.
- Preserve unrelated working-tree edits. Plan execution produces one atomic commit per task and performs no push, pull-request, or merge operation; any later merge must use squash merge.
</context>

<interfaces>
<!-- Existing contracts the executor should use directly. -->

From `pkg/api/v2/rank.go`:

```go
type Rank interface {
	Operand
	Multiply(operand Operand) Rank
	Sub(operand Operand) Rank
	Add(operand Operand) Rank
	Div(operand Operand) Rank
	Negate() Rank
	Abs() Rank
	Exp() Rank
	Log() Rank
	Max(operand Operand) Rank
	Min(operand Operand) Rank
	MarshalJSON() ([]byte, error)
	UnmarshalJSON(b []byte) error
}

type KnnRank struct {
	Query        interface{}
	Key          Key
	Limit        int
	DefaultScore *float64
	ReturnRank   bool
}

func marshalRank(rank Rank, depth int) ([]byte, error)
func (k *KnnRank) MarshalJSON() ([]byte, error)
func (r *RrfRank) Validate() error
```

`marshalRank` already rejects nil/typed-nil children, applies `MaxExpressionDepth`, dispatches exact built-in composites recursively, and falls back to a caller-defined rank's `MarshalJSON` when that custom rank is nested under a built-in expression.

From `pkg/api/v2/search.go`:

```go
type SearchFilter struct {
	IDs   []DocumentID `json:"-"`
	Where WhereClause  `json:"-"`
}

type SearchRequest struct {
	Filter  *SearchFilter
	Rank    Rank
	GroupBy *GroupBy
}

func (o *searchFilterOption) ApplyToSearchRequest(req *SearchRequest) error
func (o *rankOption) ApplyToSearchRequest(req *SearchRequest) error
func isNilInterface(v any) bool
func isNilRank(rank Rank) bool
func NewSearchRequest(opts ...SearchRequestOption) SearchCollectionOption
```

From `pkg/api/v2/where.go`:

```go
type WhereClause interface {
	Operator() WhereFilterOperator
	Key() string
	Operand() interface{}
	String() string
	Validate() error
	MarshalJSON() ([]byte, error)
	UnmarshalJSON(b []byte) error
}
```

From `pkg/api/v2/search_test.go`:

```go
type mapBackedRank map[string]string
```

`mapBackedRank` is the existing caller-defined Rank test double. Use it to prove that direct custom ranks and valid custom children of built-in expressions remain accepted.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate exact built-in rank trees before assignment</name>
  <files>pkg/api/v2/rank.go, pkg/api/v2/search.go, pkg/api/v2/rank_test.go, pkg/api/v2/search_test.go</files>
  <behavior>
    - "A zero-value KnnRank fails validation because its query and key are empty and its limit is zero"
    - "Nil and typed-nil KNN queries, an empty KNN key, a limit below one, and the existing unsupported query type all fail without producing rank JSON"
    - "Constructor-built text, dense-vector, and sparse-vector KNN ranks remain valid and keep their existing JSON"
    - "WithRank rejects zero-value KNN, invalid RRF, invalid nested KNN, and typed-nil nested rank input before replacing an existing request rank"
    - "Nested typed-nil rank errors continue to match ErrNilRank"
    - "A valid mapBackedRank remains accepted directly and as a child of a built-in expression"
    - "NewSearchRequest does not append a search when its rank option fails validation"
  </behavior>
  <action>
    Implement D-01 with the least duplicate code per D-04. Start by adding failing regressions. In `rank_test.go`, add `TestKnnRankValidation` covering a zero-value `KnnRank`, an untyped nil query, a typed-nil `*embeddings.SparseVector` query, an empty key, `Limit` below one, and the existing unsupported query type. Also prove that constructor-built text, populated dense-vector, and populated sparse-vector ranks still marshal with their current wire format. In `search_test.go`, extend `TestWithRank` with table-driven invalid exact built-ins: `&KnnRank{}`, `&RrfRank{}`, a built-in composition containing `&KnnRank{}`, and a composition containing a typed-nil built-in child. Pre-seed `req.Rank` with a valid rank and require the same value remains after every error; require the nested typed-nil case to match `ErrNilRank`. Add invalid-rank coverage through `NewSearchRequest` and require `SearchQuery.Searches` remains empty. Reuse `mapBackedRank` to prove valid custom input is accepted both directly and beneath `Val(0).Add(custom)`.

    In `rank.go`, add documented `(*KnnRank).Validate() error`. Reject nil and typed-nil `Query` values using the existing nillable-kind-aware helper, accept only `string`, `[]float32`, and non-nil `*embeddings.SparseVector`, require non-empty `Key`, and require `Limit >= 1`. Preserve the current unsupported-query-type wording. Call `Validate` at the start of `KnnRank.MarshalJSON`; let `encoding/json` continue to reject NaN and Inf rather than duplicating encoder rules.

    Add unexported `validateBuiltInRank(rank Rank) error` adjacent to `marshalRank`. Use an exact concrete type switch containing every built-in pointer type: `UnknownRank`, `ValRank`, `SumRank`, `SubRank`, `MulRank`, `DivRank`, `AbsRank`, `ExpRank`, `LogRank`, `MaxRank`, `MinRank`, `KnnRank`, and `RrfRank`. For those exact types, call `marshalRank(rank, 0)` and discard the bytes; return nil for any other implementation. Do not use an `interface{ Validate() error }` assertion, because it would change caller-defined behavior, and do not add `Validate` to `Rank`.

    In `rankOption.ApplyToSearchRequest`, retain the `isNilRank`/`ErrNilRank` check first, call `validateBuiltInRank` second, and assign `req.Rank` only after validation succeeds. Update the `WithRank` Go documentation to state that malformed built-in structures fail when the option is applied while caller-defined implementations retain their interface contract. Keep direct struct-field assignment semantics unchanged. This task is the first atomic implementation commit.
  </action>
  <verify>
    <automated>go test -tags=basicv2 ./pkg/api/v2 -run '^(TestKnnRankValidation|TestWithRank|TestWithRankNilNonPointerImplementation)$' -count=1 -timeout=60s</automated>
  </verify>
  <done>All exact built-in rank forms traverse the established recursive validation path before assignment, malformed KNN/RRF/nested ranks fail without request mutation or query append, nested nil remains ErrNilRank, and valid standalone or nested custom Rank implementations remain accepted without changing the Rank interface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate filter trees eagerly and prove public search fails before send</name>
  <files>pkg/api/v2/search.go, pkg/api/v2/search_test.go, pkg/api/v2/collection_http_test.go</files>
  <behavior>
    - "WithSearchFilter rejects a direct empty-key Where clause and an empty-key clause nested in And/Or before replacing an existing request filter"
    - "An empty SearchFilter, an IDs-only filter, and filters with untyped or typed-nil Where values remain accepted"
    - "NewSearchRequest does not append a search when its filter option fails validation"
    - "Collection.Search returns error applying search option for invalid rank and filter options, does not report error sending search request, and sends zero HTTP requests"
  </behavior>
  <action>
    Implement D-02 and the request-path coverage selected under D-04. Begin in `search_test.go` by extending `TestSearchFilter`: cover a direct invalid clause such as `EqString(K(""), "x")` and a nested invalid clause beneath `And` or `Or`; pre-seed `req.Filter` with a valid pointer and require that exact pointer remains after each error. Require the error to retain the existing `invalid search filter` context. Add accepted cases for `&SearchFilter{}`, IDs-only input, untyped nil `Where`, and typed-nil `*WhereClauseString`; require the supplied filter pointer is assigned and its current JSON remains valid. Add invalid-filter coverage through `NewSearchRequest` and require no append.

    In `searchFilterOption.ApplyToSearchRequest`, retain `ErrNilFilter` as the first check. When `o.filter.Where` is non-nil according to `isNilInterface`, call its existing recursive `Validate`; wrap failures with `invalid search filter` to retain the context currently produced during `SearchFilter.MarshalJSON`. Assign `req.Filter` only after validation succeeds. Do not allocate, normalize, or mutate the supplied filter, and do not change `SearchFilter.MarshalJSON` or `WithSearchWhere`. Update the `WithSearchFilter` Go documentation to describe eager non-nil Where validation and the accepted empty/IDs-only behavior.

    In `collection_http_test.go`, add `TestCollectionSearchRejectsInvalidOptionsBeforeSend`. Use one `httptest.Server` with a concurrency-safe request counter and table-drive an invalid zero-value KNN rank plus an invalid nested search filter through `collection.Search(context.Background(), NewSearchRequest(...))`. For both, require a nil result, an error containing `error applying search option`, no `error sending search request` text, and a zero request count. This pins D-03 at the public boundary without editing `collection_http.go`, `client.go`, or HTTP error wrapping. This task is the second atomic implementation commit.
  </action>
  <verify>
    <automated>go test -tags=basicv2 ./pkg/api/v2 -run '^(TestSearchFilter|TestTypedNilWhereBypassesOptionPipeline|TestCollectionSearchRejectsInvalidOptionsBeforeSend)$' -count=1 -timeout=60s</automated>
  </verify>
  <done>Every non-nil Where tree is recursively validated before assignment, all locked empty/IDs/nil compatibility cases pass, invalid filters never append a request, and both issue paths return as option errors before any HTTP request while HTTP wrapping remains unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-defined or built-in Rank -> WithRank | A public interface can carry malformed built-in trees, typed nils, or caller-controlled custom serialization |
| SearchFilter.Where -> WithSearchFilter | A caller-controlled recursive filter tree can contain invalid keys or invalid nested clauses |
| Applied Search options -> HTTP request | Invalid option state must not cross into embedding, serialization, or transport |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-g9r-01 | Tampering | `rankOption.ApplyToSearchRequest` | mitigate | Validate exact built-in rank trees through the existing recursive marshal boundary before assignment, while preserving the custom Rank contract |
| T-g9r-02 | Denial of Service | Recursive rank validation | mitigate | Reuse existing nil, depth, term-count, division-by-zero, RRF, and JSON encoding guards instead of introducing an unbounded walker |
| T-g9r-03 | Tampering | `searchFilterOption.ApplyToSearchRequest` | mitigate | Invoke recursive `WhereClause.Validate` before assignment and pin nested invalid input plus no-mutation behavior |
| T-g9r-04 | Repudiation | `CollectionImpl.Search` error attribution | mitigate | Request-path tests distinguish option-application failure from send failure and prove the HTTP handler is never reached |
| T-g9r-SC | Tampering | Package installation | accept | The plan adds no dependency or package-manager operation |
</threat_model>

<verification>
1. `go test -tags=basicv2 ./pkg/api/v2 -run '^(TestKnnRankValidation|TestWithRank|TestWithRankNilNonPointerImplementation|TestSearchFilter|TestTypedNilWhereBypassesOptionPipeline|TestCollectionSearchRejectsInvalidOptionsBeforeSend)$' -count=1 -timeout=60s`
2. `go test -tags=basicv2 ./pkg/api/v2 -count=1 -timeout=120s`
3. `make lint`
4. `git diff --check`
5. Inspect `git diff -- pkg/api/v2/rank.go pkg/api/v2/search.go pkg/api/v2/rank_test.go pkg/api/v2/search_test.go pkg/api/v2/collection_http_test.go` and confirm it contains only eager validation, contract comments, and focused regressions; `pkg/api/v2/collection_http.go` and `pkg/api/v2/client.go` remain unchanged.
6. Confirm `git status --short` contains no unexpected task-created files and leaves all unrelated edits intact.
</verification>

<source_coverage_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Review issue 516, validate its claims, and implement the smallest complete fix with regressions | 01 | COVERED | Research validated the claims; Tasks 1-2 implement both option boundaries and public request-path coverage |
| REQ | GH-516 | Reject malformed Rank and SearchFilter option input eagerly instead of surfacing it as a later send failure | 01 | COVERED | Task 1 covers rank; Task 2 covers filters and no-send attribution |
| RESEARCH | R-01 | Reuse one exact-built-in rank validation boundary backed by `marshalRank` | 01 | COVERED | Task 1 enumerates every built-in pointer type and preserves custom implementations |
| RESEARCH | R-02 | Add KNN query, key, and limit validation at its serialization boundary | 01 | COVERED | Task 1 adds `KnnRank.Validate` and calls it from `MarshalJSON` |
| RESEARCH | R-03 | Validate a non-nil `SearchFilter.Where` directly and preserve nil/typed-nil omission | 01 | COVERED | Task 2 mirrors `WithSearchWhere`'s nillable-kind-aware boundary |
| RESEARCH | R-04 | Prove invalid options fail before HTTP send | 01 | COVERED | Task 2 adds a public `Collection.Search` regression with a zero request counter |
| RESEARCH | R-05 | Leave shared HTTP error classification unchanged | — | EXCLUDED | Research found no narrow behavior-preserving discriminator; D-03 and the task constraints explicitly keep wrapping outside this change |
| CONTEXT | D-01 | Recursively validate built-in ranks, preserve custom Rank compatibility, and reject before assignment | 01 | COVERED | Task 1 |
| CONTEXT | D-02 | Eagerly validate non-nil Where trees while preserving empty, IDs-only, nil, and typed-nil behavior | 01 | COVERED | Task 2 |
| CONTEXT | D-03 | Make eager option validation required and change HTTP wrapping only if research finds a clean discriminator | 01 | COVERED | Tasks reject before send; researched wrapper expansion is explicitly excluded |
| CONTEXT | D-04 | Choose the smallest internal validator and focused mutation/timing tests | 01 | COVERED | Tasks reuse `marshalRank`, `WhereClause.Validate`, existing test doubles, and one HTTP counter |
</source_coverage_audit>

<success_criteria>
- `WithRank` rejects zero-value KNN, invalid RRF, malformed nested built-ins, and nested nils before changing the request or appending a search.
- KNN validation requires a non-nil supported query, non-empty key, and positive limit while valid text, dense, and sparse ranks retain their wire format.
- The public Rank interface is unchanged, direct valid custom ranks are not eagerly marshaled, and valid custom children remain supported.
- `WithSearchFilter` recursively rejects invalid non-nil Where trees before assignment while empty, IDs-only, nil-Where, and typed-nil-Where filters remain valid.
- Invalid rank and filter options return from `Collection.Search` as option-application errors with zero HTTP requests and no HTTP wrapper changes.
- Focused tests, the full tagged V2 package suite, lint, and diff checks pass with changes limited to the five planned files.
</success_criteria>

<output>
Create `.planning/quick/260801-g9r-review-validate-and-address-the-scope-of/260801-g9r-SUMMARY.md` when done.
</output>

---
phase: quick-260731-tq1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/api/v2/rank.go
  - pkg/api/v2/rank_test.go
autonomous: true
requirements:
  - GH-499

must_haves:
  truths:
    - "Passing an untyped nil Operand to rank arithmetic no longer silently substitutes Val(0)"
    - "RrfRank Add, Multiply, Sub, Div, Max, and Min with nil all build without panic and fail during MarshalJSON with the existing UnknownRank programming-error message"
    - "Typed-nil Rank operands continue to fail with ErrNilRank, while IntOperand, FloatOperand, and unsupported non-Rank operands retain their current behavior"
  artifacts:
    - path: "pkg/api/v2/rank.go"
      provides: "Fail-closed nil conversion at the shared operandToRank boundary and accurate public contract comments"
      contains: "func operandToRank(operand Operand) Rank"
    - path: "pkg/api/v2/rank_test.go"
      provides: "Focused conversion and RRF arithmetic regressions for untyped nil operands"
      contains: "TestRrfRankUntypedNilOperandMarshal"
  key_links:
    - from: "pkg/api/v2/rank.go:operandToRank"
      to: "pkg/api/v2/rank.go:UnknownRank.MarshalJSON"
      via: "an untyped nil is converted to the existing erroring sentinel instead of Val(0)"
      pattern: "return &UnknownRank\\{\\}"
    - from: "pkg/api/v2/rank_test.go:TestRrfRankUntypedNilOperandMarshal"
      to: "pkg/api/v2/rank.go:operandToRank"
      via: "each public binary RRF method reaches the shared conversion helper before composite marshaling"
      pattern: "r\\.(Add|Multiply|Sub|Div|Max|Min)\\(nil\\)"
---

<objective>
Make nil rank operands fail visibly instead of silently changing rank arithmetic to use zero.

Purpose: issue 499 identifies a programming error that currently produces valid but unintended rank JSON. The shared conversion boundary can expose that error consistently for every Rank implementation without per-method changes.

Output: one conversion change, corrected contract comments, and focused regression coverage for conversion plus all six RRF binary arithmetic methods.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@pkg/api/v2/rank.go
@pkg/api/v2/rank_test.go

**Working directory:** `/Users/tazarov/GolandProjects/chroma-go`

**Verified baseline:** On branch `fix/issue-499`, `operandToRank(nil)` returns `Val(0)`, and the existing `TestOperandConversion/untyped_nil_operand_becomes_zero` regression passes with that behavior.

**Scope boundaries:**
- Change only the shared untyped-nil conversion and the comments/tests that encode its contract; all binary Rank methods already delegate to `operandToRank`.
- Preserve the typed-nil Rank path: `isNilRank(v)` returns true and `operandToRank` returns nil so composite marshaling reports `ErrNilRank`.
- Preserve IntOperand, FloatOperand, non-nil Rank, and unsupported non-Rank conversion behavior.
- Reuse `UnknownRank` and its current marshal error; do not add a new public error, dependency, reflection helper, or per-Rank nil guard.
- Do not broaden the change into search request handling, RRF validation, wire-format changes for valid expressions, or unrelated cleanup.
- Preserve unrelated working-tree edits. Do not push, open a PR, or merge as part of plan execution; any eventual PR merge must be squash-only.
</context>

<interfaces>
<!-- Existing contracts the executor should use directly. -->

From `pkg/api/v2/rank.go`:

```go
type Operand interface {
	IsOperand()
}

type Rank interface {
	Operand
	Multiply(operand Operand) Rank
	Sub(operand Operand) Rank
	Add(operand Operand) Rank
	Div(operand Operand) Rank
	Max(operand Operand) Rank
	Min(operand Operand) Rank
	MarshalJSON() ([]byte, error)
}

type UnknownRank struct {
	ValRank
}

func (u *UnknownRank) MarshalJSON() ([]byte, error)
func operandToRank(operand Operand) Rank
```

`UnknownRank.MarshalJSON` already returns an error containing both `unknown operand type` and `programming error`. `TestRankArithmeticTypedNilOperandMarshal` already pins typed-nil operands to `ErrNilRank`; keep that test unchanged and passing.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fail closed for untyped nil rank operands</name>
  <files>pkg/api/v2/rank.go, pkg/api/v2/rank_test.go</files>
  <behavior>
    - "operandToRank(nil) produces *UnknownRank rather than *ValRank"
    - "RrfRank Add(nil), Multiply(nil), Sub(nil), Div(nil), Max(nil), and Min(nil) never panic while building or marshaling and each marshal returns the existing UnknownRank programming-error message"
    - "The untyped-nil error is not ErrNilRank; typed-nil Rank operands remain ErrNilRank"
    - "Valid integer, float, and Rank operands retain their existing JSON output"
  </behavior>
  <action>
    Start in `pkg/api/v2/rank_test.go`. Replace the `TestOperandConversion` subtest named `untyped nil operand becomes zero` with a fail-closed regression: pass an untyped nil Operand through `operandToRank`, require the result to be a non-nil `*UnknownRank`, marshal it without panic, and require an error containing `unknown operand type` and `programming error` but not matching `ErrNilRank`. Keep the existing int, float, and unsupported-operand subtests intact.

    Add `TestRrfRankUntypedNilOperandMarshal`. Construct one valid RrfRank with the existing `mustNewKnnRank` and `mustNewRrfRank` helpers, then table-drive its six binary methods: Add, Multiply, Sub, Div, Max, and Min, each with literal nil. For each row, build and marshal inside `require.NotPanics`, require no JSON bytes, require the existing UnknownRank message fragments, and require `require.NotErrorIs(t, err, ErrNilRank)`. This is the public regression for the issue examples and proves that division does not merely change from silent zero substitution to the unrelated division-by-zero error. Run the focused tests before editing production and confirm they fail against the verified Val(0) baseline.

    In `pkg/api/v2/rank.go`, change only the leading `operand == nil` branch of `operandToRank` to return `&UnknownRank{}`. Do not edit the 72 binary fluent methods individually because they already use this helper. Update both the Rank interface comment near the top of the file and the `operandToRank` comment to state that untyped nil becomes UnknownRank and therefore fails during marshaling as a programming error; retain the separate typed-nil-to-ErrNilRank contract. Leave the switch cases and `UnknownRank.MarshalJSON` unchanged.
  </action>
  <verify>
    <automated>go test -tags=basicv2 ./pkg/api/v2 -run '^(TestOperandConversion|TestRrfRankUntypedNilOperandMarshal|TestRankArithmeticTypedNilOperandMarshal)$' -count=1 -timeout=30s</automated>
  </verify>
  <done>Untyped nil converts to UnknownRank, all six RRF binary operations surface its programming-error marshal failure without panic, typed-nil operands still return ErrNilRank, and valid/unsupported operand regressions pass unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller Operand -> fluent Rank expression | A caller-controlled interface value may be untyped nil and currently crosses into a valid arithmetic expression as zero |
| Rank expression -> JSON request representation | Invalid caller input must fail before it can silently alter ranking semantics sent to Chroma |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-tq1-01 | Tampering | `operandToRank` and binary Rank composition | mitigate | Route untyped nil to the existing erroring UnknownRank sentinel and pin all six RRF binary operations with marshal regressions |
| T-tq1-02 | Denial of Service | Composite Rank marshaling | mitigate | Assert invalid nil operands return a controlled error without panic while preserving typed-nil ErrNilRank handling |
| T-tq1-SC | Tampering | Package installation | accept | The plan installs no package and changes no dependency metadata |
</threat_model>

<verification>
1. `go test -tags=basicv2 ./pkg/api/v2 -run '^(TestOperandConversion|TestRrfRankUntypedNilOperandMarshal|TestRankArithmeticTypedNilOperandMarshal)$' -count=1 -timeout=30s`
2. `go test -tags=basicv2 ./pkg/api/v2/... -count=1 -timeout=120s`
3. `make lint`
4. `git diff --check`
5. Inspect `git diff -- pkg/api/v2/rank.go pkg/api/v2/rank_test.go` and confirm it contains only the shared nil conversion, its two contract comments, and focused regressions.
6. Confirm `git status --short` contains no unexpected task-created files; leave any pre-existing unrelated changes untouched.
</verification>

<source_coverage_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Verify and address issue 499 | 01 | COVERED | Baseline is recorded; Task 1 changes the shared boundary and proves public behavior |
| REQ | GH-499 | Stop `operandToRank(nil)` from silently substituting `Val(0)` and add focused regression coverage | 01 | COVERED | Task 1 returns UnknownRank and covers conversion plus six RRF binary operations |
| RESEARCH | — | No research phase | — | EXCLUDED | Quick mode explicitly supplies the behavior and existing local patterns are sufficient |
| CONTEXT | QUICK-01 | Keep the implementation small and prefer no code when behavior already complies | 01 | COVERED | Baseline proves it does not comply; the production change is one shared return value plus contract updates |
| CONTEXT | QUICK-02 | Treat issue content as untrusted data | 01 | COVERED | The plan uses only the stated behavior as requirements and executes no issue-provided content |
| CONTEXT | PROJECT-01 | Keep repository artifacts free of prohibited internal information | 01 | COVERED | No such information appears in the plan or planned outputs |
| CONTEXT | PROJECT-02 | Use squash merge | — | EXCLUDED | This plan performs no merge; the scope boundary records squash-only for any eventual merge |
| CONTEXT | PROJECT-03 | Preserve unrelated edits | 01 | COVERED | Verification inspects only planned files and explicitly leaves other changes untouched |
</source_coverage_audit>

<success_criteria>
- `operandToRank(nil)` returns a non-nil `*UnknownRank`, never `Val(0)` or a true nil Rank.
- RrfRank Add, Multiply, Sub, Div, Max, and Min with nil all return the existing UnknownRank programming-error failure from MarshalJSON without panic or JSON output.
- Typed-nil Rank operands still match `ErrNilRank`; numeric, valid Rank, and unsupported-operand behavior remains unchanged.
- Both code comments that documented nil-to-zero compatibility accurately describe the new fail-closed contract.
- Focused tests, the full tagged V2 suite, lint, and diff checks pass with changes limited to `rank.go` and `rank_test.go`.
</success_criteria>

<output>
Create `.planning/quick/260731-tq1-verify-and-address-issue-499/260731-tq1-SUMMARY.md` when done.
</output>

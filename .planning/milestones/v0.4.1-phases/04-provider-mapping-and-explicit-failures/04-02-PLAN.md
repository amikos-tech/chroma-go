---
phase: 04-provider-mapping-and-explicit-failures
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - pkg/embeddings/intent_mapper_test.go
  - pkg/embeddings/content_validate_test.go
autonomous: true
requirements:
  - MAP-01
  - MAP-02

must_haves:
  truths:
    - "Mock IntentMapper maps neutral intents to predictable native strings"
    - "IsNeutralIntent returns true for all 5 neutral constants and false for custom strings"
    - "ValidateContentSupport rejects unsupported modality with unsupported_modality code"
    - "ValidateContentSupport rejects unsupported neutral intent with unsupported_intent code"
    - "ValidateContentSupport rejects unsupported dimension with unsupported_dimension code"
    - "ValidateContentSupport passes through when CapabilityMetadata has no declared capabilities"
    - "Custom intents bypass the intent pre-check even when caps.Intents is non-empty"
    - "ValidateContentsSupport fails on the first unsupported item in a batch"
  artifacts:
    - path: "pkg/embeddings/intent_mapper_test.go"
      provides: "IntentMapper contract tests, IsNeutralIntent tests, escape hatch tests"
      contains: "func TestIntentMapper"
    - path: "pkg/embeddings/content_validate_test.go"
      provides: "ValidateContentSupport and ValidateContentsSupport tests"
      contains: "func TestValidateContentSupport"
  key_links:
    - from: "pkg/embeddings/intent_mapper_test.go"
      to: "pkg/embeddings/embedding.go"
      via: "stubIntentMapper implements IntentMapper"
      pattern: "var _ IntentMapper"
    - from: "pkg/embeddings/content_validate_test.go"
      to: "pkg/embeddings/multimodal_validate.go"
      via: "calls ValidateContentSupport and ValidateContentsSupport"
      pattern: "ValidateContentSupport\\("
---

<objective>
Add comprehensive test coverage for the IntentMapper contract, IsNeutralIntent helper, ValidateContentSupport pre-check, and ValidateContentsSupport batch behavior.

Purpose: Prove MAP-01 (neutral intent mapping with tests) and MAP-02 (explicit unsupported-combination failures) with automated verification before downstream provider adoption in Phases 6-7.

Output: Two new test files in pkg/embeddings/ covering all mapping and validation scenarios from the Phase 4 research test map.
</objective>

<execution_context>
@/Users/tazarov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-provider-mapping-and-explicit-failures/04-CONTEXT.md
@.planning/phases/04-provider-mapping-and-explicit-failures/04-RESEARCH.md
@.planning/phases/04-provider-mapping-and-explicit-failures/04-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 output that tests must exercise. -->

From pkg/embeddings/embedding.go (added in Plan 01):
```go
type IntentMapper interface {
    MapIntent(intent Intent) (string, error)
}
```

From pkg/embeddings/multimodal.go (added in Plan 01):
```go
func IsNeutralIntent(intent Intent) bool
```

From pkg/embeddings/multimodal_validate.go (added in Plan 01):
```go
const (
    validationCodeUnsupportedModality  = "unsupported_modality"
    validationCodeUnsupportedIntent    = "unsupported_intent"
    validationCodeUnsupportedDimension = "unsupported_dimension"
)
func ValidateContentSupport(content Content, caps CapabilityMetadata) error
func ValidateContentsSupport(contents []Content, caps CapabilityMetadata) error
```

From pkg/embeddings/capabilities_test.go (test helper to reuse):
```go
func requireValidationIssue(t *testing.T, err error, path, code, messagePart string)
```

From pkg/embeddings/capabilities_test.go (stub pattern to follow):
```go
type capabilityAwareStubEmbeddingFunction struct {
    caps CapabilityMetadata
}
// compile-time check pattern:
var _ IntentMapper = (*stubIntentMapper)(nil)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add IntentMapper and IsNeutralIntent tests</name>
  <files>pkg/embeddings/intent_mapper_test.go</files>
  <read_first>
    - pkg/embeddings/embedding.go (IntentMapper interface added in Plan 01)
    - pkg/embeddings/multimodal.go (IsNeutralIntent function added in Plan 01, 5 intent constants)
    - pkg/embeddings/capabilities_test.go (requireValidationIssue helper at line 305, stub patterns)
  </read_first>
  <behavior>
    - TestIsNeutralIntent: all 5 neutral constants return true; empty string returns false; custom string "RETRIEVAL_QUERY_V2" returns false; custom string "my_custom_task" returns false
    - TestIntentMapperContract: stubIntentMapper maps IntentRetrievalQuery to "RETRIEVAL_QUERY", maps IntentSemanticSimilarity to "SEMANTIC_SIMILARITY"; unmapped neutral intent falls through to string(intent); compile-time var _ check proves interface satisfaction
    - TestIntentMapperEscapeHatch: stubIntentMapper receives custom Intent("CUSTOM_TASK") and passes it through as "CUSTOM_TASK"; stubIntentMapper can return an error for specific intents
    - TestIntentMapperNilCheck: type assertion against a non-IntentMapper value returns false (proves opt-in pattern)
  </behavior>
  <action>
Create `pkg/embeddings/intent_mapper_test.go` in `package embeddings` with:

1. A `stubIntentMapper` struct implementing `IntentMapper`:
```go
type stubIntentMapper struct {
    mappings map[Intent]string
    errs     map[Intent]error
}

func (m *stubIntentMapper) MapIntent(intent Intent) (string, error) {
    if err, ok := m.errs[intent]; ok {
        return "", err
    }
    if native, ok := m.mappings[intent]; ok {
        return native, nil
    }
    return string(intent), nil // pass-through for unmapped
}
```

2. Compile-time interface satisfaction check:
```go
var _ IntentMapper = (*stubIntentMapper)(nil)
```

3. `TestIsNeutralIntent` — table-driven test with these cases:
   - `IntentRetrievalQuery` -> true
   - `IntentRetrievalDocument` -> true
   - `IntentClassification` -> true
   - `IntentClustering` -> true
   - `IntentSemanticSimilarity` -> true
   - `Intent("")` -> false
   - `Intent("RETRIEVAL_QUERY_V2")` -> false
   - `Intent("my_custom_task")` -> false

4. `TestIntentMapperContract` — create stubIntentMapper with mappings:
   - `IntentRetrievalQuery` -> `"RETRIEVAL_QUERY"`
   - `IntentSemanticSimilarity` -> `"SEMANTIC_SIMILARITY"`
   Assert: MapIntent(IntentRetrievalQuery) returns "RETRIEVAL_QUERY", nil.
   Assert: MapIntent(IntentSemanticSimilarity) returns "SEMANTIC_SIMILARITY", nil.
   Assert: MapIntent(IntentClustering) returns "clustering" (pass-through), nil.

5. `TestIntentMapperEscapeHatch` — create stubIntentMapper with no mappings.
   Assert: MapIntent(Intent("CUSTOM_TASK")) returns "CUSTOM_TASK", nil.
   Then create stubIntentMapper with errs map: `Intent("bad_task")` -> `fmt.Errorf("unknown intent")`.
   Assert: MapIntent(Intent("bad_task")) returns "", error containing "unknown intent".

6. `TestIntentMapperNilCheck` — create a `recordingEmbeddingFunction` (from capabilities_test.go same package) and type-assert to IntentMapper:
   Assert: `_, ok := ef.(IntentMapper)` is false. Proves opt-in pattern.
  </action>
  <verify>
    <automated>cd /Users/tazarov/GolandProjects/chroma-go && go test ./pkg/embeddings/ -run 'TestIsNeutralIntent|TestIntentMapper' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - pkg/embeddings/intent_mapper_test.go contains `var _ IntentMapper = (*stubIntentMapper)(nil)`
    - pkg/embeddings/intent_mapper_test.go contains `func TestIsNeutralIntent(t *testing.T)`
    - pkg/embeddings/intent_mapper_test.go contains `func TestIntentMapperContract(t *testing.T)`
    - pkg/embeddings/intent_mapper_test.go contains `func TestIntentMapperEscapeHatch(t *testing.T)`
    - pkg/embeddings/intent_mapper_test.go contains `func TestIntentMapperNilCheck(t *testing.T)`
    - pkg/embeddings/intent_mapper_test.go contains `type stubIntentMapper struct`
    - `go test ./pkg/embeddings/ -run 'TestIsNeutralIntent|TestIntentMapper' -count=1` exits 0
  </acceptance_criteria>
  <done>IntentMapper contract is exercised with stub, IsNeutralIntent covers all 5 constants plus custom strings, escape hatch pass-through and error injection work, opt-in type assertion pattern is proven</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ValidateContentSupport and ValidateContentsSupport tests</name>
  <files>pkg/embeddings/content_validate_test.go</files>
  <read_first>
    - pkg/embeddings/multimodal_validate.go (ValidateContentSupport, ValidateContentsSupport, 3 new codes added in Plan 01)
    - pkg/embeddings/capabilities.go (CapabilityMetadata struct, RequestOptionDimension constant)
    - pkg/embeddings/multimodal.go (Content, Part, Intent, Modality types, IsNeutralIntent)
    - pkg/embeddings/capabilities_test.go (requireValidationIssue helper at line 305)
    - pkg/embeddings/multimodal_compat.go (NewTextPart, NewPartFromSource, NewBinarySourceFromURL helpers)
  </read_first>
  <behavior>
    - TestValidateContentSupportModality: content with audio part against caps declaring only text+image returns unsupported_modality error at "parts[0].modality"
    - TestValidateContentSupportIntent: content with IntentClassification against caps declaring only retrieval_query+semantic_similarity returns unsupported_intent error at "intent"
    - TestValidateContentSupportDimension: content with Dimension=128 against caps with RequestOptions not including dimension returns unsupported_dimension error at "dimension"
    - TestValidateContentSupportPassThrough: content with audio part against empty CapabilityMetadata{} returns nil (pass-through)
    - TestValidateContentSupportCustomIntentBypass: content with custom Intent("CUSTOM") against caps with declared Intents returns nil (custom intents bypass)
    - TestValidateContentSupportDimensionPassThrough: content with Dimension=128 against caps with empty RequestOptions returns nil
    - TestValidateContentSupportMultipleIssues: content with unsupported modality AND unsupported intent accumulates both issues
    - TestValidateContentsSupportBatch: batch of 3 items where 2nd has unsupported modality -- returns error prefixed with "contents[1]."
    - TestValidateContentsSupportEmptyBatch: empty batch returns nil
  </behavior>
  <action>
Create `pkg/embeddings/content_validate_test.go` in `package embeddings` with:

1. `TestValidateContentSupportModality`:
   - caps: `CapabilityMetadata{Modalities: []Modality{ModalityText, ModalityImage}}`
   - content: `Content{Parts: []Part{NewPartFromSource(ModalityAudio, NewBinarySourceFromURL("https://example.com/audio.wav"))}}`
   - Assert error via `requireValidationIssue(t, err, "parts[0].modality", validationCodeUnsupportedModality, "audio")`

2. `TestValidateContentSupportIntent`:
   - caps: `CapabilityMetadata{Intents: []Intent{IntentRetrievalQuery, IntentSemanticSimilarity}}`
   - content: `Content{Parts: []Part{NewTextPart("text")}, Intent: IntentClassification}`
   - Assert error via `requireValidationIssue(t, err, "intent", validationCodeUnsupportedIntent, "classification")`

3. `TestValidateContentSupportDimension`:
   - caps: `CapabilityMetadata{RequestOptions: []RequestOption{RequestOptionProviderHints}}` (has options but NOT dimension)
   - dim := 128; content: `Content{Parts: []Part{NewTextPart("text")}, Dimension: &dim}`
   - Assert error via `requireValidationIssue(t, err, "dimension", validationCodeUnsupportedDimension, "dimension override")`

4. `TestValidateContentSupportPassThrough`:
   - caps: `CapabilityMetadata{}` (empty -- no modalities, no intents, no options)
   - content: `Content{Parts: []Part{NewPartFromSource(ModalityAudio, NewBinarySourceFromURL("https://example.com/audio.wav"))}, Intent: IntentClassification}`
   - Assert `require.NoError(t, err)` -- everything passes through

5. `TestValidateContentSupportCustomIntentBypass`:
   - caps: `CapabilityMetadata{Intents: []Intent{IntentRetrievalQuery}}` (declares intents but not custom)
   - content: `Content{Parts: []Part{NewTextPart("text")}, Intent: Intent("CUSTOM_TASK")}`
   - Assert `require.NoError(t, err)` -- custom intent bypasses enforcement

6. `TestValidateContentSupportDimensionPassThrough`:
   - caps: `CapabilityMetadata{}` (empty RequestOptions)
   - dim := 128; content: `Content{Parts: []Part{NewTextPart("text")}, Dimension: &dim}`
   - Assert `require.NoError(t, err)` -- empty options means pass-through

7. `TestValidateContentSupportMultipleIssues`:
   - caps: `CapabilityMetadata{Modalities: []Modality{ModalityText}, Intents: []Intent{IntentRetrievalQuery}}`
   - content: `Content{Parts: []Part{NewPartFromSource(ModalityAudio, NewBinarySourceFromURL("https://example.com/a.wav"))}, Intent: IntentClassification}`
   - Assert err is `*ValidationError` with `len(Issues) == 2`
   - Assert Issues[0].Code == validationCodeUnsupportedModality
   - Assert Issues[1].Code == validationCodeUnsupportedIntent

8. `TestValidateContentsSupportBatch`:
   - caps: `CapabilityMetadata{Modalities: []Modality{ModalityText}}`
   - contents: 3 items: text, audio (unsupported), text
   - Assert error contains "contents[1]." prefix via `requireValidationIssue(t, err, "contents[1].parts[0].modality", validationCodeUnsupportedModality, "audio")`

9. `TestValidateContentsSupportEmptyBatch`:
   - caps: `CapabilityMetadata{Modalities: []Modality{ModalityText}}`
   - contents: empty slice
   - Assert `require.NoError(t, err)`
  </action>
  <verify>
    <automated>cd /Users/tazarov/GolandProjects/chroma-go && go test ./pkg/embeddings/ -run 'TestValidateContentSupport|TestValidateContentsSupport' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - pkg/embeddings/content_validate_test.go contains `func TestValidateContentSupportModality(t *testing.T)`
    - pkg/embeddings/content_validate_test.go contains `func TestValidateContentSupportIntent(t *testing.T)`
    - pkg/embeddings/content_validate_test.go contains `func TestValidateContentSupportDimension(t *testing.T)`
    - pkg/embeddings/content_validate_test.go contains `func TestValidateContentSupportPassThrough(t *testing.T)`
    - pkg/embeddings/content_validate_test.go contains `func TestValidateContentSupportCustomIntentBypass(t *testing.T)`
    - pkg/embeddings/content_validate_test.go contains `func TestValidateContentsSupportBatch(t *testing.T)`
    - pkg/embeddings/content_validate_test.go contains `func TestValidateContentsSupportEmptyBatch(t *testing.T)`
    - pkg/embeddings/content_validate_test.go contains `validationCodeUnsupportedModality`
    - pkg/embeddings/content_validate_test.go contains `validationCodeUnsupportedIntent`
    - pkg/embeddings/content_validate_test.go contains `validationCodeUnsupportedDimension`
    - `go test ./pkg/embeddings/ -run 'TestValidateContentSupport|TestValidateContentsSupport' -count=1` exits 0
  </acceptance_criteria>
  <done>All ValidateContentSupport scenarios covered: unsupported modality, intent, dimension; pass-through for empty caps; custom intent bypass; multiple issue accumulation; batch fail-on-first with prefixed paths; empty batch returns nil</done>
</task>

</tasks>

<verification>
All Phase 4 tests pass:
```bash
cd /Users/tazarov/GolandProjects/chroma-go && go test ./pkg/embeddings/ -run 'TestIntentMapper|TestIsNeutralIntent|TestValidateContentSupport|TestValidateContentsSupport' -count=1 -v
```

Full package tests still pass (no regressions):
```bash
cd /Users/tazarov/GolandProjects/chroma-go && go test ./pkg/embeddings/ -count=1
```

Lint clean:
```bash
cd /Users/tazarov/GolandProjects/chroma-go && make lint
```
</verification>

<success_criteria>
- intent_mapper_test.go exercises IntentMapper contract with stub, tests IsNeutralIntent for all 5 constants and custom strings, proves escape hatch pass-through, proves opt-in type assertion pattern
- content_validate_test.go covers all 8 ValidateContentSupport scenarios from the research test map: unsupported modality/intent/dimension, pass-through, custom intent bypass, dimension pass-through, multiple issues, batch fail-on-first, and empty batch
- All tests pass with `go test ./pkg/embeddings/ -count=1`
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-provider-mapping-and-explicit-failures/04-02-SUMMARY.md`
</output>

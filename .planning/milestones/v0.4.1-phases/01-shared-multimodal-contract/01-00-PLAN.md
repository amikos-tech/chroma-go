---
phase: 01-shared-multimodal-contract
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - pkg/embeddings/multimodal_test.go
  - pkg/embeddings/multimodal_validation_test.go
autonomous: true
requirements:
  - MMOD-01
  - MMOD-02
  - MMOD-03
  - MMOD-04
  - MMOD-05
user_setup: []
must_haves:
  truths:
    - "Wave 0 automated verification exists before the implementation plans begin."
    - "The planned multimodal test names compile and can be targeted by later plan verify commands."
  artifacts:
    - path: pkg/embeddings/multimodal_test.go
      provides: Wave 0 stubs for positive-path multimodal contract tests
      min_lines: 40
      contains: "TestMultimodalContentSupportsAllModalities"
    - path: pkg/embeddings/multimodal_validation_test.go
      provides: Wave 0 stubs for validation and compatibility tests
      min_lines: 40
      contains: "TestMultimodalValidationErrors"
  key_links:
    - from: pkg/embeddings/multimodal_test.go
      to: .planning/phases/01-shared-multimodal-contract/01-VALIDATION.md
      via: Stub test names match the validation strategy task map
      pattern: "TestMultimodalContentPreservesOrder"
    - from: pkg/embeddings/multimodal_validation_test.go
      to: .planning/phases/01-shared-multimodal-contract/01-VALIDATION.md
      via: Stub test names match the validation strategy task map
      pattern: "TestNewImagePartFromImageInput"
---

<objective>
Create Wave 0 test scaffolding for the multimodal contract phase.

Purpose: Satisfy the Nyquist validation contract before implementation work starts and give later plans stable automated targets.
Output: Compileable stub test files with the exact test names referenced by `01-VALIDATION.md`.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/01-shared-multimodal-contract/01-CONTEXT.md
@.planning/phases/01-shared-multimodal-contract/01-RESEARCH.md
@.planning/phases/01-shared-multimodal-contract/01-VALIDATION.md
@docs/docs/embeddings.md
@pkg/embeddings/embedding.go
@pkg/embeddings/registry.go
@pkg/api/v2/configuration.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compileable Wave 0 stubs for positive-path multimodal tests</name>
  <files>pkg/embeddings/multimodal_test.go</files>
  <read_first>pkg/embeddings/embedding.go, pkg/embeddings/registry.go, pkg/api/v2/configuration.go, .planning/phases/01-shared-multimodal-contract/01-VALIDATION.md, .planning/phases/01-shared-multimodal-contract/01-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/research/SUMMARY.md, .planning/codebase/CONCERNS.md, .planning/codebase/ARCHITECTURE.md, docs/docs/embeddings.md</read_first>
  <action>Create `pkg/embeddings/multimodal_test.go` with Go `testing` package and `testify/require` imports. Add compileable stub tests named `TestMultimodalContentSupportsAllModalities`, `TestMultimodalContentPreservesOrder`, and `TestMultimodalRequestOptions`. Use `t.Skip("phase 1 implementation plan fills this test")` inside each stub so the file compiles and the test names become stable automated targets without forcing implementation in Wave 0.</action>
  <verify>rg -n "func TestMultimodalContentSupportsAllModalities|func TestMultimodalContentPreservesOrder|func TestMultimodalRequestOptions|t\\.Skip\\(\"phase 1 implementation plan fills this test\"\\)" pkg/embeddings/multimodal_test.go && go test ./pkg/embeddings -run 'TestMultimodalContentSupportsAllModalities|TestMultimodalContentPreservesOrder|TestMultimodalRequestOptions'</verify>
  <acceptance_criteria>
    - `pkg/embeddings/multimodal_test.go` contains `func TestMultimodalContentSupportsAllModalities`
    - `pkg/embeddings/multimodal_test.go` contains `func TestMultimodalContentPreservesOrder`
    - `pkg/embeddings/multimodal_test.go` contains `func TestMultimodalRequestOptions`
    - `pkg/embeddings/multimodal_test.go` contains `t.Skip("phase 1 implementation plan fills this test")`
    - `go test ./pkg/embeddings -run 'TestMultimodalContentSupportsAllModalities|TestMultimodalContentPreservesOrder|TestMultimodalRequestOptions'` exits 0
  </acceptance_criteria>
  <done>The positive-path multimodal test targets exist and compile before implementation begins.</done>
</task>

<task type="auto">
  <name>Task 2: Add compileable Wave 0 stubs for validation and compatibility tests</name>
  <files>pkg/embeddings/multimodal_validation_test.go</files>
  <read_first>pkg/embeddings/embedding.go, pkg/embeddings/registry.go, pkg/api/v2/configuration.go, .planning/phases/01-shared-multimodal-contract/01-VALIDATION.md, .planning/phases/01-shared-multimodal-contract/01-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/research/SUMMARY.md, .planning/codebase/CONCERNS.md, .planning/codebase/ARCHITECTURE.md, docs/docs/embeddings.md</read_first>
  <action>Create `pkg/embeddings/multimodal_validation_test.go` with Go `testing` package and `testify/require` imports. Add compileable stub tests named `TestMultimodalIntentValidation`, `TestMultimodalValidationErrors`, and `TestNewImagePartFromImageInput`. Use the same `t.Skip("phase 1 implementation plan fills this test")` pattern so later plans can replace the bodies while all verify commands already point at stable test names.</action>
  <verify>rg -n "func TestMultimodalIntentValidation|func TestMultimodalValidationErrors|func TestNewImagePartFromImageInput|t\\.Skip\\(\"phase 1 implementation plan fills this test\"\\)" pkg/embeddings/multimodal_validation_test.go && go test ./pkg/embeddings -run 'TestMultimodalIntentValidation|TestMultimodalValidationErrors|TestNewImagePartFromImageInput'</verify>
  <acceptance_criteria>
    - `pkg/embeddings/multimodal_validation_test.go` contains `func TestMultimodalIntentValidation`
    - `pkg/embeddings/multimodal_validation_test.go` contains `func TestMultimodalValidationErrors`
    - `pkg/embeddings/multimodal_validation_test.go` contains `func TestNewImagePartFromImageInput`
    - `pkg/embeddings/multimodal_validation_test.go` contains `t.Skip("phase 1 implementation plan fills this test")`
    - `go test ./pkg/embeddings -run 'TestMultimodalIntentValidation|TestMultimodalValidationErrors|TestNewImagePartFromImageInput'` exits 0
  </acceptance_criteria>
  <done>The validation and compatibility test targets exist and compile before implementation begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./pkg/embeddings -run 'TestMultimodalContentSupportsAllModalities|TestMultimodalContentPreservesOrder|TestMultimodalRequestOptions|TestMultimodalIntentValidation|TestMultimodalValidationErrors|TestNewImagePartFromImageInput'`
- [ ] `go test -tags=basicv2 ./pkg/api/v2 -run '^TestBuildEmbeddingFunctionFromConfig$'`
- [ ] Stub test names match the verification map in `01-VALIDATION.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Wave 0 test files exist before plans `01-01` and `01-02`
- Later plans can target named tests instead of ad hoc grep-only verification
  </success_criteria>

<output>
After completion, create `.planning/phases/01-shared-multimodal-contract/01-00-SUMMARY.md`
</output>

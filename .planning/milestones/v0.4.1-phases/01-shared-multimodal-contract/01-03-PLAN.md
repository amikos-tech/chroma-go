---
phase: 01-shared-multimodal-contract
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-00"
  - "01-01"
  - "01-02"
files_modified:
  - pkg/embeddings/multimodal_test.go
  - pkg/embeddings/multimodal_validation_test.go
autonomous: true
requirements:
  - MMOD-01
  - MMOD-02
  - MMOD-03
  - MMOD-04
  - MMOD-05
user_setup: []
must_haves:
  truths:
    - "Unit tests prove the shared contract supports text, image, audio, video, and PDF parts."
    - "Unit tests prove ordered parts and batch content ordering are preserved."
    - "Unit tests prove request-time options and validation errors behave explicitly before provider I/O."
  artifacts:
    - path: pkg/embeddings/multimodal_test.go
      provides: Positive-path tests for contract construction and ordering
      min_lines: 120
      contains: "TestMultimodalContentPreservesOrder"
    - path: pkg/embeddings/multimodal_validation_test.go
      provides: Validation and compatibility helper tests
      min_lines: 120
      contains: "TestMultimodalValidationErrors"
  key_links:
    - from: pkg/embeddings/multimodal_test.go
      to: pkg/embeddings/multimodal.go
      via: Tests instantiate Content, Part, BinarySource, and request options
      pattern: "Content\\{"
    - from: pkg/embeddings/multimodal_validation_test.go
      to: pkg/embeddings/multimodal_validate.go
      via: Tests assert ValidationError issue paths and codes
      pattern: "ValidationError"
---

<objective>
Lock down the new shared multimodal contract with focused unit tests.

Purpose: Prove the Phase 1 contract works for all required modalities, preserves order, and fails invalid shapes explicitly before provider I/O.
Output: Positive-path and validation-focused test suites for the new shared contract.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/01-shared-multimodal-contract/01-CONTEXT.md
@.planning/phases/01-shared-multimodal-contract/01-RESEARCH.md
@.planning/phases/01-shared-multimodal-contract/01-VALIDATION.md
@docs/docs/embeddings.md
@pkg/embeddings/embedding.go
@pkg/embeddings/registry.go
@pkg/api/v2/configuration.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add positive-path tests for modality coverage, ordering, and request options</name>
  <files>pkg/embeddings/multimodal_test.go</files>
  <read_first>pkg/embeddings/multimodal.go, pkg/embeddings/multimodal_compat.go, pkg/embeddings/registry.go, pkg/api/v2/configuration.go, .planning/REQUIREMENTS.md, .planning/research/SUMMARY.md, .planning/codebase/CONCERNS.md, .planning/codebase/ARCHITECTURE.md, docs/docs/embeddings.md, .planning/phases/01-shared-multimodal-contract/01-VALIDATION.md, .planning/phases/01-shared-multimodal-contract/01-CONTEXT.md</read_first>
  <action>Create `pkg/embeddings/multimodal_test.go` using Go `testing` and `testify/require`. Add `TestMultimodalContentSupportsAllModalities` to construct valid text, image, audio, video, and PDF parts and assert they validate successfully. Add `TestMultimodalContentPreservesOrder` to build a mixed ordered `Content` and a `[]Content` batch, then assert part order and batch order are unchanged after helper construction and validation. Add `TestMultimodalRequestOptions` to assert `Intent`, `Dimension`, and `ProviderHints` remain attached to the request object without using `context.Context` helpers.</action>
  <verify>rg -n "func TestMultimodalContentSupportsAllModalities|func TestMultimodalContentPreservesOrder|func TestMultimodalRequestOptions" pkg/embeddings/multimodal_test.go && go test ./pkg/embeddings -run 'TestMultimodalContentSupportsAllModalities|TestMultimodalContentPreservesOrder|TestMultimodalRequestOptions'</verify>
  <acceptance_criteria>
    - `pkg/embeddings/multimodal_test.go` contains `func TestMultimodalContentSupportsAllModalities`
    - `pkg/embeddings/multimodal_test.go` contains `func TestMultimodalContentPreservesOrder`
    - `pkg/embeddings/multimodal_test.go` contains `func TestMultimodalRequestOptions`
    - `go test ./pkg/embeddings -run 'TestMultimodalContentSupportsAllModalities|TestMultimodalContentPreservesOrder|TestMultimodalRequestOptions'` exits 0
  </acceptance_criteria>
  <done>`multimodal_test.go` proves the positive-path contract covers all required modalities, preserves order, and keeps request options on `Content`.</done>
</task>

<task type="auto">
  <name>Task 2: Add validation and compatibility tests with structured error assertions</name>
  <files>pkg/embeddings/multimodal_validation_test.go</files>
  <read_first>pkg/embeddings/multimodal.go, pkg/embeddings/multimodal_validate.go, pkg/embeddings/multimodal_compat.go, pkg/embeddings/embedding.go, pkg/embeddings/registry.go, pkg/api/v2/configuration.go, .planning/REQUIREMENTS.md, .planning/research/SUMMARY.md, .planning/codebase/CONCERNS.md, .planning/codebase/ARCHITECTURE.md, docs/docs/embeddings.md, .planning/phases/01-shared-multimodal-contract/01-VALIDATION.md</read_first>
  <action>Create `pkg/embeddings/multimodal_validation_test.go` using Go `testing` and `testify/require`. Add `TestMultimodalIntentValidation` to cover empty intent, shared neutral intent constants, whitespace-only custom intents, and raw custom intent strings. Add `TestMultimodalValidationErrors` to assert `require.ErrorAs` unwraps `*ValidationError` and that issue paths/codes are populated for empty content, empty parts, invalid source combinations, and invalid dimensions. Add a compatibility-focused test that `NewImagePartFromImageInput` preserves URL, file path, and base64 sources without performing I/O and that invalid `ImageInput` values fail via shared validation rather than panicking.</action>
  <verify>rg -n "func TestMultimodalIntentValidation|func TestMultimodalValidationErrors|func TestNewImagePartFromImageInput|require\\.ErrorAs" pkg/embeddings/multimodal_validation_test.go && go test ./pkg/embeddings -run 'TestMultimodalIntentValidation|TestMultimodalValidationErrors|TestNewImagePartFromImageInput'</verify>
  <acceptance_criteria>
    - `pkg/embeddings/multimodal_validation_test.go` contains `func TestMultimodalIntentValidation`
    - `pkg/embeddings/multimodal_validation_test.go` contains `func TestMultimodalValidationErrors`
    - `pkg/embeddings/multimodal_validation_test.go` contains `func TestNewImagePartFromImageInput`
    - `pkg/embeddings/multimodal_validation_test.go` contains `require.ErrorAs`
    - `go test ./pkg/embeddings -run 'TestMultimodalIntentValidation|TestMultimodalValidationErrors|TestNewImagePartFromImageInput'` exits 0
  </acceptance_criteria>
  <done>`multimodal_validation_test.go` locks down typed validation errors and the legacy image compatibility helper behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./pkg/embeddings`
- [ ] `go test -tags=basicv2 ./pkg/api/v2 -run '^TestBuildEmbeddingFunctionFromConfig$'`
- [ ] `make test`
- [ ] Test files assert structured validation issues, not only raw error strings
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Tests cover all Phase 1 requirements MMOD-01 through MMOD-05
- Validation tests assert typed issues and compatibility helper behavior
- Package-level tests stay green without changing collection or config behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/01-shared-multimodal-contract/01-03-SUMMARY.md`
</output>

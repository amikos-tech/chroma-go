---
phase: 02-capability-metadata-and-compatibility
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - pkg/embeddings/capabilities_test.go
  - pkg/embeddings/roboflow/roboflow_test.go
  - pkg/api/v2/configuration_test.go
autonomous: true
requirements:
  - CAPS-01
  - CAPS-02
  - COMP-01
  - COMP-02
user_setup: []
must_haves:
  truths:
    - "Tests prove capability metadata is inspectable through shared interfaces rather than provider concrete types."
    - "Tests prove legacy text-only and image-only compatibility paths still work and unsupported shared-content shapes fail explicitly."
    - "Dense config reconstruction and collection configuration behavior remain unchanged after adding Phase 2 interfaces."
  artifacts:
    - path: pkg/embeddings/capabilities_test.go
      provides: Shared capability and compatibility adapter regression tests
      min_lines: 140
      contains: "TestCapabilityMetadata"
    - path: pkg/embeddings/roboflow/roboflow_test.go
      provides: Provider capability and shared-content delegation tests
      min_lines: 360
      contains: "TestRoboflowCapabilities"
    - path: pkg/api/v2/configuration_test.go
      provides: Legacy config reconstruction regression tests
      min_lines: 650
      contains: "TestBuildEmbeddingFunctionFromConfig"
  key_links:
    - from: pkg/embeddings/capabilities_test.go
      to: pkg/embeddings/capabilities.go
      via: Tests assert capability metadata and adapter behavior through shared types
      pattern: "CapabilityMetadata"
    - from: pkg/api/v2/configuration_test.go
      to: pkg/embeddings/embedding.go
      via: Regression tests prove dense config reconstruction is unaffected by additive capability interfaces
      pattern: "SetEmbeddingFunction"
---

<objective>
Lock down the Phase 2 capability and compatibility work with focused regression tests.

Purpose: Prove capability inspection is shared and additive, prove legacy text/image callers still work, and prove config reconstruction stays stable after the new interfaces are introduced.
Output: Shared capability tests, provider-level regression coverage, and V2 config regression tests.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@.planning/phases/02-capability-metadata-and-compatibility/02-RESEARCH.md
@.planning/phases/02-capability-metadata-and-compatibility/02-VALIDATION.md
@CLAUDE.md
@pkg/embeddings/capabilities.go
@pkg/embeddings/embedding.go
@pkg/embeddings/multimodal.go
@pkg/embeddings/multimodal_compat.go
@pkg/embeddings/roboflow/roboflow.go
@pkg/embeddings/roboflow/roboflow_test.go
@pkg/api/v2/configuration.go
@pkg/api/v2/configuration_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared capability and compatibility adapter tests</name>
  <files>pkg/embeddings/capabilities_test.go</files>
  <read_first>pkg/embeddings/capabilities.go, pkg/embeddings/embedding.go, pkg/embeddings/multimodal.go, pkg/embeddings/multimodal_compat.go, pkg/embeddings/registry.go, .planning/REQUIREMENTS.md, .planning/codebase/TESTING.md, .planning/phases/02-capability-metadata-and-compatibility/02-RESEARCH.md, .planning/phases/02-capability-metadata-and-compatibility/02-VALIDATION.md, CLAUDE.md</read_first>
  <action>Create `pkg/embeddings/capabilities_test.go` using Go `testing` and `testify/require`. Add `TestCapabilityMetadata` to assert shared capability metadata exposes modalities, intents, and request options through the shared `CapabilityAware` interface only. Add `TestLegacyTextCompatibility` using a small in-test stub `EmbeddingFunction` to verify `AdaptEmbeddingFunctionToContent` accepts exactly one text part and delegates to the legacy text methods. Add `TestLegacyImageCompatibility` using a stub `MultimodalEmbeddingFunction` to verify `AdaptMultimodalEmbeddingFunctionToContent` accepts single text or image content. Add `TestCompatibilityAdapterRejectsUnsupportedContent` to assert explicit failures for mixed-part content, audio/video/PDF content, and requests carrying `Intent`, `Dimension`, or `ProviderHints` on legacy adapter paths.</action>
  <verify>rg -n "func TestCapabilityMetadata|func TestLegacyTextCompatibility|func TestLegacyImageCompatibility|func TestCompatibilityAdapterRejectsUnsupportedContent" pkg/embeddings/capabilities_test.go && go test ./pkg/embeddings -run 'TestCapabilityMetadata|TestLegacyTextCompatibility|TestLegacyImageCompatibility|TestCompatibilityAdapterRejectsUnsupportedContent'</verify>
  <acceptance_criteria>
    - `pkg/embeddings/capabilities_test.go` contains `func TestCapabilityMetadata`
    - `pkg/embeddings/capabilities_test.go` contains `func TestLegacyTextCompatibility`
    - `pkg/embeddings/capabilities_test.go` contains `func TestLegacyImageCompatibility`
    - `pkg/embeddings/capabilities_test.go` contains `func TestCompatibilityAdapterRejectsUnsupportedContent`
    - `go test ./pkg/embeddings -run 'TestCapabilityMetadata|TestLegacyTextCompatibility|TestLegacyImageCompatibility|TestCompatibilityAdapterRejectsUnsupportedContent'` exits 0
  </acceptance_criteria>
  <done>`capabilities_test.go` proves shared capability discovery and legacy compatibility adapter behavior with explicit unsupported-case coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Add provider and config regression coverage for the additive capability surface</name>
  <files>pkg/embeddings/roboflow/roboflow_test.go, pkg/api/v2/configuration_test.go</files>
  <read_first>pkg/embeddings/roboflow/roboflow.go, pkg/embeddings/roboflow/roboflow_test.go, pkg/embeddings/capabilities.go, pkg/embeddings/multimodal_compat.go, pkg/api/v2/configuration.go, pkg/api/v2/configuration_test.go, .planning/REQUIREMENTS.md, .planning/codebase/TESTING.md, .planning/phases/02-capability-metadata-and-compatibility/02-RESEARCH.md, .planning/phases/02-capability-metadata-and-compatibility/02-VALIDATION.md, CLAUDE.md</read_first>
  <action>Update `pkg/embeddings/roboflow/roboflow_test.go` to add `TestRoboflowCapabilities`, asserting that a `RoboflowEmbeddingFunction` can be treated as `embeddings.CapabilityAware` and that its metadata exposes text and image modalities with `SupportsMixedPart == false`. Add `TestRoboflowContentCompatibility` to cover `EmbedContent` and `EmbedContents` for single text and single image requests plus explicit rejection of mixed-part content. Update `pkg/api/v2/configuration_test.go` with a regression test named `TestBuildEmbeddingFunctionFromConfig_IgnoresCapabilityExtensions` that verifies the additive capability interfaces do not change the stored `embedding_function` shape or the result of `BuildEmbeddingFunctionFromConfig`, and that `SetEmbeddingFunction` still persists only `type`, `name`, and `config` for dense providers.</action>
  <verify>rg -n "func TestRoboflowCapabilities|func TestRoboflowContentCompatibility|func TestBuildEmbeddingFunctionFromConfig_IgnoresCapabilityExtensions" pkg/embeddings/roboflow/roboflow_test.go pkg/api/v2/configuration_test.go && go test ./pkg/embeddings/roboflow -run 'TestRoboflowCapabilities|TestRoboflowContentCompatibility' && go test -tags=basicv2 ./pkg/api/v2 -run '^TestBuildEmbeddingFunctionFromConfig$|^TestBuildEmbeddingFunctionFromConfig_IgnoresCapabilityExtensions$|^TestCollectionConfiguration_(Get|Set)EmbeddingFunction$'</verify>
  <acceptance_criteria>
    - `pkg/embeddings/roboflow/roboflow_test.go` contains `func TestRoboflowCapabilities`
    - `pkg/embeddings/roboflow/roboflow_test.go` contains `func TestRoboflowContentCompatibility`
    - `pkg/api/v2/configuration_test.go` contains `func TestBuildEmbeddingFunctionFromConfig_IgnoresCapabilityExtensions`
    - `go test ./pkg/embeddings/roboflow -run 'TestRoboflowCapabilities|TestRoboflowContentCompatibility'` exits 0
    - `go test -tags=basicv2 ./pkg/api/v2 -run '^TestBuildEmbeddingFunctionFromConfig$|^TestBuildEmbeddingFunctionFromConfig_IgnoresCapabilityExtensions$|^TestCollectionConfiguration_(Get|Set)EmbeddingFunction$'` exits 0
  </acceptance_criteria>
  <done>Provider-level and V2 config regression tests prove the additive Phase 2 surface does not break legacy callers or config reconstruction.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./pkg/embeddings`
- [ ] `go test ./pkg/embeddings/roboflow`
- [ ] `go test -tags=basicv2 ./pkg/api/v2 -run '^TestBuildEmbeddingFunctionFromConfig$|^TestBuildEmbeddingFunctionFromConfig_IgnoresCapabilityExtensions$|^TestCollectionConfiguration_(Get|Set)EmbeddingFunction$'`
- [ ] `make test`
- [ ] Tests assert explicit unsupported-case failures and shared capability inspection
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Capability discovery is tested through shared interfaces only
- Legacy text/image compatibility paths are regression-covered
- Dense config reconstruction remains stable after additive capability work
  </success_criteria>

<output>
After completion, create `.planning/phases/02-capability-metadata-and-compatibility/02-03-SUMMARY.md`
</output>

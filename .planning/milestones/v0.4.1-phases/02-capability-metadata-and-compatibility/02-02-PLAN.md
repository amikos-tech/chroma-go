---
phase: 02-capability-metadata-and-compatibility
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - pkg/embeddings/multimodal_compat.go
  - pkg/embeddings/roboflow/roboflow.go
autonomous: true
requirements:
  - CAPS-01
  - COMP-01
  - COMP-02
user_setup: []
must_haves:
  truths:
    - "Legacy text-only and image-only callers continue to use their existing interfaces unchanged."
    - "Compatibility adapters only accept shared Content shapes that can be represented safely by the legacy interfaces."
    - "Roboflow exposes shared capability metadata and a limited shared-content delegation path for supported single text or single image requests."
  artifacts:
    - path: pkg/embeddings/multimodal_compat.go
      provides: Compatibility adapters between legacy interfaces and shared Content requests
      min_lines: 120
      contains: "AdaptEmbeddingFunctionToContent"
    - path: pkg/embeddings/roboflow/roboflow.go
      provides: First concrete provider capability implementation and shared-content delegation
      min_lines: 260
      contains: "Capabilities() embeddings.CapabilityMetadata"
  key_links:
    - from: pkg/embeddings/multimodal_compat.go
      to: pkg/embeddings/embedding.go
      via: Adapters implement ContentEmbeddingFunction using legacy interfaces
      pattern: "ContentEmbeddingFunction"
    - from: pkg/embeddings/roboflow/roboflow.go
      to: pkg/embeddings/capabilities.go
      via: Provider advertises shared modalities and request-option support
      pattern: "CapabilityMetadata"
---

<objective>
Introduce compatibility adapters and provider delegation paths for the new shared capability surface.

Purpose: Keep current text-only and image-only callers working unchanged while making supported shared-content requests possible through additive compatibility layers.
Output: Shared compatibility adapters plus a concrete provider implementation that exposes capabilities and limited shared-content delegation.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@.planning/phases/02-capability-metadata-and-compatibility/02-RESEARCH.md
@.planning/phases/02-capability-metadata-and-compatibility/02-VALIDATION.md
@CLAUDE.md
@pkg/embeddings/embedding.go
@pkg/embeddings/capabilities.go
@pkg/embeddings/multimodal.go
@pkg/embeddings/multimodal_compat.go
@pkg/embeddings/multimodal_validate.go
@pkg/embeddings/roboflow/roboflow.go
@pkg/embeddings/roboflow/roboflow_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared-content compatibility adapters for legacy text and image interfaces</name>
  <files>pkg/embeddings/multimodal_compat.go</files>
  <read_first>pkg/embeddings/multimodal_compat.go, pkg/embeddings/embedding.go, pkg/embeddings/capabilities.go, pkg/embeddings/multimodal.go, pkg/embeddings/multimodal_validate.go, pkg/embeddings/roboflow/roboflow.go, .planning/REQUIREMENTS.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/CONCERNS.md, .planning/codebase/TESTING.md, .planning/phases/02-capability-metadata-and-compatibility/02-RESEARCH.md, .planning/phases/02-capability-metadata-and-compatibility/02-VALIDATION.md, CLAUDE.md</read_first>
  <action>Extend `pkg/embeddings/multimodal_compat.go` with additive compatibility adapters named `AdaptEmbeddingFunctionToContent(ef EmbeddingFunction, caps CapabilityMetadata) ContentEmbeddingFunction` and `AdaptMultimodalEmbeddingFunctionToContent(ef MultimodalEmbeddingFunction, caps CapabilityMetadata) ContentEmbeddingFunction`. Implement them with concrete adapter structs that validate `Content` before delegation and only accept shared request shapes that the legacy interfaces can represent safely. Enforce these exact rules: adapters require exactly one `Part`; adapters reject non-empty `Intent`, non-nil `Dimension`, and non-empty `ProviderHints`; the text adapter only accepts `ModalityText`; the multimodal adapter accepts `ModalityText` and `ModalityImage`; image conversion must reuse existing `ImageInput`/`BinarySource` provenance helpers; unsupported mixed-part, audio, video, or PDF content must return explicit errors instead of silently dropping fields or coercing modalities.</action>
  <verify>rg -n "AdaptEmbeddingFunctionToContent|AdaptMultimodalEmbeddingFunctionToContent|exactly one Part|ProviderHints|Dimension|Intent" pkg/embeddings/multimodal_compat.go && go test ./pkg/embeddings ./pkg/embeddings/roboflow</verify>
  <acceptance_criteria>
    - `pkg/embeddings/multimodal_compat.go` contains `AdaptEmbeddingFunctionToContent`
    - `pkg/embeddings/multimodal_compat.go` contains `AdaptMultimodalEmbeddingFunctionToContent`
    - `pkg/embeddings/multimodal_compat.go` contains logic referencing `Intent`, `Dimension`, and `ProviderHints`
    - `pkg/embeddings/multimodal_compat.go` contains logic that rejects unsupported modality cases
    - `go test ./pkg/embeddings ./pkg/embeddings/roboflow` exits 0
  </acceptance_criteria>
  <done>`multimodal_compat.go` can bridge safe single-part text/image requests into the legacy interfaces and fails unsupported shared-content shapes explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Expose shared capabilities and supported shared-content delegation on Roboflow</name>
  <files>pkg/embeddings/roboflow/roboflow.go</files>
  <read_first>pkg/embeddings/roboflow/roboflow.go, pkg/embeddings/roboflow/roboflow_test.go, pkg/embeddings/embedding.go, pkg/embeddings/capabilities.go, pkg/embeddings/multimodal.go, pkg/embeddings/multimodal_compat.go, .planning/REQUIREMENTS.md, .planning/phases/02-capability-metadata-and-compatibility/02-RESEARCH.md, .planning/phases/02-capability-metadata-and-compatibility/02-VALIDATION.md, CLAUDE.md</read_first>
  <action>Update `pkg/embeddings/roboflow/roboflow.go` so `RoboflowEmbeddingFunction` implements `embeddings.CapabilityAware` and additive shared-content delegation. Add a `Capabilities() embeddings.CapabilityMetadata` method that advertises `Modalities: []embeddings.Modality{embeddings.ModalityText, embeddings.ModalityImage}`, empty or nil shared `Intents`, empty or nil shared `RequestOptions`, `SupportsBatch: true`, and `SupportsMixedPart: false`. Add `EmbedContent(ctx context.Context, content embeddings.Content) (embeddings.Embedding, error)` and `EmbedContents(ctx context.Context, contents []embeddings.Content) ([]embeddings.Embedding, error)` that delegate through the compatibility rules from `pkg/embeddings/multimodal_compat.go`: single text part routes to `EmbedQuery`/`EmbedDocuments`, single image part routes to `EmbedImage`/`EmbedImages`, unsupported shapes return explicit errors, and existing `EmbedQuery`, `EmbedDocuments`, `EmbedImage`, and `EmbedImages` behavior stays unchanged.</action>
  <verify>rg -n "Capabilities\\(\\) embeddings.CapabilityMetadata|SupportsMixedPart: false|embeddings.ModalityText|embeddings.ModalityImage|EmbedContent\\(ctx context.Context, content embeddings.Content\\)|EmbedContents\\(ctx context.Context, contents \\[\\]embeddings.Content\\)" pkg/embeddings/roboflow/roboflow.go && go test ./pkg/embeddings/roboflow</verify>
  <acceptance_criteria>
    - `pkg/embeddings/roboflow/roboflow.go` contains `Capabilities() embeddings.CapabilityMetadata`
    - `pkg/embeddings/roboflow/roboflow.go` contains `SupportsMixedPart: false`
    - `pkg/embeddings/roboflow/roboflow.go` contains `embeddings.ModalityText`
    - `pkg/embeddings/roboflow/roboflow.go` contains `embeddings.ModalityImage`
    - `pkg/embeddings/roboflow/roboflow.go` contains `EmbedContent(ctx context.Context, content embeddings.Content)`
    - `pkg/embeddings/roboflow/roboflow.go` contains `EmbedContents(ctx context.Context, contents []embeddings.Content)`
    - `go test ./pkg/embeddings/roboflow` exits 0
  </acceptance_criteria>
  <done>`roboflow.go` becomes the first concrete provider to advertise shared capabilities and support safe single-text/single-image shared-content delegation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./pkg/embeddings`
- [ ] `go test ./pkg/embeddings/roboflow`
- [ ] `go test -tags=basicv2 ./pkg/api/v2 -run '^TestBuildEmbeddingFunctionFromConfig$'`
- [ ] `rg -n "AdaptEmbeddingFunctionToContent|AdaptMultimodalEmbeddingFunctionToContent|Capabilities\\(\\) embeddings.CapabilityMetadata|EmbedContent\\(" pkg/embeddings/multimodal_compat.go pkg/embeddings/roboflow/roboflow.go`
- [ ] Unsupported mixed-part and unsupported-modality requests fail explicitly rather than being coerced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Legacy text-only and image-only caller paths remain unchanged
- Shared compatibility adapters only accept safe representable content shapes
- Roboflow exposes shared capabilities and a constrained shared-content path
  </success_criteria>

<output>
After completion, create `.planning/phases/02-capability-metadata-and-compatibility/02-02-SUMMARY.md`
</output>
